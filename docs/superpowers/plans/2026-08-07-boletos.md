# Área de Boletos — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deixar síndico/administração/gerente subir um único PDF com N boletos (um por unidade), a plataforma casar automaticamente cada página com a unidade certa (CPF como critério principal, texto de unidade como reforço), o síndico revisar/corrigir antes de publicar, e o morador ver/baixar seus boletos pelo app — com notificação push na publicação.

**Architecture:** Módulo novo e autocontido (`Boletos`/`Finance`), sem alterar LPR/CFTV/Wallet. Duas entidades (`BoletoBatch`, `BoletoDocument`) persistidas via EF Core seguindo o padrão já usado em `Operations` (DTO = entidade EF). PDF armazenado cifrado em disco (mesmo esquema AES-GCM do `PrivateMediaStore`, arquivo por página). Algoritmo de casamento é uma função pura e testável, isolada de I/O. Upload/revisão só no portal web (staff); visualização só no app mobile (morador) — não existe portal web de morador nesta plataforma.

**Tech Stack:** ASP.NET Core 8 API + EF Core/Npgsql, Blazor Server (MudBlazor) para o portal web, MAUI Blazor Hybrid para o app. Duas bibliotecas novas, ambas OSS permissivas: `UglyToad.PdfPig` (leitura/extração de texto) e `PdfSharp` (divisão do PDF em páginas individuais).

## Global Constraints

- Spec de referência: `docs/superpowers/specs/2026-08-07-boletos-design.md` — qualquer conflito entre este plano e a spec, a spec governa (perguntar ao humano antes de divergir).
- Upload/revisão/publicação: só portal web (`Condotify`), atrás de `LicensePermissionEnum.ManageFinance`. Visualização/download: só app mobile (`Condotify.Mobile`), atrás de `[Authorize(Policy = "Resident")]`. Não existe portal web de morador — não criar nenhuma tela resident-facing no projeto `Condotify`.
- Nenhuma extração de valor/vencimento por página — `Reference` e `DueDate` são informados uma vez, manualmente, para o lote inteiro.
- Nenhuma integração de pagamento (Pix, gateway, baixa automática) nesta entrega.
- A função de casamento (CPF/texto → unidade) é pura, sem I/O, e mora separada do código que lê o PDF — é o único lugar que precisa de cobertura de teste pesada.
- Sem provider de banco em teste (só Postgres/Npgsql) — testes de controller seguem o padrão de reflexão de atributos/rotas já usado no projeto (ver `CondotifyAPI.Tests/ManagementCrudEndpointTests.cs`, `ResidentProfileControllerTests.cs`).
- Todo texto de UI em português, seguindo o tom já usado no resto do app.

---

## File Structure

**Backend (`CondotifyAPI*`):**
- `CondotifyAPI.Domain/DTO/Finance/BoletoDtos.cs` — `BoletoBatchDTO`, `BoletoDocumentDTO`, `BoletoBatchStatusEnum`, `BoletoMatchMethodEnum` (entidades EF, seguindo o padrão de `SafetyOperationsDTO.cs`).
- `CondotifyAPI.Infrastructure/ContextConfiguration/Finance/BoletoConfiguration.cs` — `IEntityTypeConfiguration` das duas entidades.
- `CondotifyAPI.Infrastructure/DatabaseContext/Finance/DatabaseContext.Boleto.cs` — `DbSet`s + método de registro, partial de `DatabaseContext`.
- `CondotifyAPI/Services/Finance/BoletoDocumentStore.cs` — armazenamento cifrado do PDF de cada página.
- `CondotifyAPI/Services/Finance/BoletoPageMatcher.cs` — algoritmo puro de casamento CPF/texto → unidade.
- `CondotifyAPI/Services/Finance/BoletoPdfProcessor.cs` — extração de texto e divisão em páginas (PdfPig + PdfSharp).
- `CondotifyAPI/Data/Finance/BoletoDtos.cs` — contratos de request/response da API (`...In`/`...Out`).
- `CondotifyAPI/Controllers/BoletosController.cs` — endpoints de staff (upload, listagem, revisão, publicação, cancelamento, exclusão).
- `CondotifyAPI/Controllers/ResidentBoletosController.cs` — endpoints de morador (listagem, download).
- `CondotifyAPI.Tests/BoletoPageMatcherTests.cs`, `CondotifyAPI.Tests/BoletosControllerTests.cs`, `CondotifyAPI.Tests/ResidentBoletosControllerTests.cs`.

**Contratos compartilhados:**
- `Condotify.Contracts/FinanceViewModels.cs` — `BoletoBatchViewModel`, `BoletoDocumentViewModel`, `ResidentBoletoViewModel`.

**Portal web (`Condotify`):**
- `Condotify/Components/LicenseModules/BoletosModule.razor` — lista de lotes + explicativo animado de primeiro acesso.
- `Condotify/Components/Dialogs/BoletoUploadDialog.razor` — formulário de novo lote.
- `Condotify/Components/Dialogs/BoletoReviewDialog.razor` — grid de revisão/publicação.
- `Condotify.ApiClient/CondotifyApiClient.cs` — métodos novos (staff).
- `Condotify/wwwroot/css/portal.css` — estilos novos (`.boletos-*`).

**App mobile (`Condotify.Mobile`):**
- `Condotify.Mobile/Components/Pages/Boletos.razor` — lista do morador.
- `Condotify.Mobile/Components/Pages/More.razor` — item de navegação novo.
- `Condotify.Mobile/Components/Pages/Notifications.razor` — categoria nova no rótulo/ícone.
- `Condotify.Mobile/wwwroot/css/app.css` — estilos novos (`.boletos-*`).

---

### Task 1: Permissões, categoria de notificação e rota de deep link

**Files:**
- Modify: `CondotifyAPI.Domain/Enums/License/LicenseAccessEnums.cs`
- Modify: `Condotify.Contracts/LicenseManagementViewModels.cs`
- Modify: `CondotifyAPI.Domain/Enums/Mobile/MobileNotificationEnums.cs`
- Modify: `Condotify.Contracts/MobileNotificationViewModels.cs`

**Interfaces:**
- Produces: `LicensePermissionEnum.ViewFinance` / `.ManageFinance` (valor `1L << 31` / `1L << 32`), espelhados em `LicensePermission.ViewFinance` / `.ManageFinance` (mesmo valor). `MobileNotificationCategory.Financial = 8` em ambas as definições. Rota `/boletos` aceita por `MobileDeepLinks.TryNormalize`.

- [ ] **Step 1: Adicionar as permissões no enum da API**

Em `CondotifyAPI.Domain/Enums/License/LicenseAccessEnums.cs`, trocar:

```csharp
    ViewVehicles = 1L << 29,
    ManageVehicles = 1L << 30,
    All = (1L << 31) - 1
}
```

por:

```csharp
    ViewVehicles = 1L << 29,
    ManageVehicles = 1L << 30,
    ViewFinance = 1L << 31,
    ManageFinance = 1L << 32,
    All = (1L << 33) - 1
}
```

E, no método `Normalize` (mesmo arquivo), adicionar uma linha junto das demais (logo após a de `ManageEmergency`):

```csharp
        if (permissions.HasFlag(LicensePermissionEnum.ManageFinance)) permissions |= LicensePermissionEnum.ViewFinance;
```

- [ ] **Step 2: Espelhar no enum do portal web**

Em `Condotify.Contracts/LicenseManagementViewModels.cs`, trocar:

```csharp
        ManageEmergency = 1L << 28,
        All = (1L << 29) - 1
    }
```

por:

```csharp
        ManageEmergency = 1L << 28,
        ViewFinance = 1L << 31,
        ManageFinance = 1L << 32,
        All = (1L << 33) - 1
    }
```

(Os bits 29/30 de `ViewVehicles`/`ManageVehicles` não têm nome neste enum hoje — não precisam ganhar um agora, o valor numérico transmitido pela API já os inclui em `All` independentemente do nome existir aqui.)

- [ ] **Step 3: Categoria de notificação — API**

Em `CondotifyAPI.Domain/Enums/Mobile/MobileNotificationEnums.cs`, trocar:

```csharp
    Operational = 6,
    System = 7
}
```

por:

```csharp
    Operational = 6,
    System = 7,
    Financial = 8
}
```

- [ ] **Step 4: Categoria de notificação — Contracts**

Em `Condotify.Contracts/MobileNotificationViewModels.cs`, aplicar a mesma troca (`Operational = 6, System = 7` → `..., Financial = 8`).

- [ ] **Step 5: Rota de deep link**

Em `Condotify.Contracts/MobileNotificationViewModels.cs`, dentro de `MobileDeepLinks`, trocar:

```csharp
    private static readonly string[] StaticRoutes = ["/home", "/profile"];
```

por:

```csharp
    private static readonly string[] StaticRoutes = ["/home", "/profile", "/boletos"];
```

- [ ] **Step 6: Build**

Run: `dotnet build CondotifyAPI.sln`
Expected: build limpo, sem erros.

- [ ] **Step 7: Commit**

```bash
git add CondotifyAPI.Domain/Enums/License/LicenseAccessEnums.cs Condotify.Contracts/LicenseManagementViewModels.cs CondotifyAPI.Domain/Enums/Mobile/MobileNotificationEnums.cs Condotify.Contracts/MobileNotificationViewModels.cs
git commit -m "feat(boletos): add ViewFinance/ManageFinance permissions and Financial notification category"
```

---

### Task 2: Modelo de dados e migração

**Files:**
- Create: `CondotifyAPI.Domain/DTO/Finance/BoletoDtos.cs`
- Create: `CondotifyAPI.Infrastructure/ContextConfiguration/Finance/BoletoConfiguration.cs`
- Create: `CondotifyAPI.Infrastructure/DatabaseContext/Finance/DatabaseContext.Boleto.cs`
- Modify: `CondotifyAPI.Infrastructure/DatabaseContext/DatabaseContext.cs`
- Create: migração EF (`CondotifyAPI.Infrastructure/Migrations/...`)

**Interfaces:**
- Produces: `BoletoBatchDTO { Id, LicenseId, License, Reference, DueDate, UploadedByUserId, UploadedByName, Status, SourceFileName, TotalPages, CreatedAt, PublishedAt, Documents }`, `BoletoDocumentDTO { Id, BatchId, Batch, UnitId, Unit, PageNumber, MatchMethod, Ignored, StorageReference, ExtractedSnippet, CreatedAt }`, `BoletoBatchStatusEnum { Processing, PendingReview, Published, Cancelled }`, `BoletoMatchMethodEnum { Cpf, UnitText, Manual, Unmatched }`.

- [ ] **Step 1: Criar as entidades EF**

Create `CondotifyAPI.Domain/DTO/Finance/BoletoDtos.cs`:

```csharp
using CondotifyAPI.Domain.DTO.License;
using CondotifyAPI.Domain.DTO.Unit;

namespace CondotifyAPI.Domain.DTO.Finance;

public sealed class BoletoBatchDTO
{
    public Guid Id { get; set; }
    public Guid LicenseId { get; set; }
    public LicenseDTO License { get; set; } = null!;
    public string Reference { get; set; } = string.Empty;
    public DateTime DueDate { get; set; }
    public Guid UploadedByUserId { get; set; }
    public string UploadedByName { get; set; } = string.Empty;
    public BoletoBatchStatusEnum Status { get; set; }
    public string SourceFileName { get; set; } = string.Empty;
    public int TotalPages { get; set; }
    public DateTime CreatedAt { get; set; }
    public DateTime? PublishedAt { get; set; }
    public List<BoletoDocumentDTO> Documents { get; set; } = [];
}

public sealed class BoletoDocumentDTO
{
    public Guid Id { get; set; }
    public Guid BatchId { get; set; }
    public BoletoBatchDTO Batch { get; set; } = null!;
    public Guid? UnitId { get; set; }
    public UnitDTO? Unit { get; set; }
    public int PageNumber { get; set; }
    public BoletoMatchMethodEnum MatchMethod { get; set; }
    public bool Ignored { get; set; }
    public string StorageReference { get; set; } = string.Empty;
    public string ExtractedSnippet { get; set; } = string.Empty;
    public DateTime CreatedAt { get; set; }
}

public enum BoletoBatchStatusEnum { Processing = 0, PendingReview = 1, Published = 2, Cancelled = 3 }
public enum BoletoMatchMethodEnum { Cpf = 0, UnitText = 1, Manual = 2, Unmatched = 3 }
```

- [ ] **Step 2: Configuração EF**

Create `CondotifyAPI.Infrastructure/ContextConfiguration/Finance/BoletoConfiguration.cs`:

```csharp
using CondotifyAPI.Domain.DTO.Finance;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace CondotifyAPI.Infrastructure.ContextConfiguration.Finance;

public sealed class BoletoBatchConfiguration : IEntityTypeConfiguration<BoletoBatchDTO>
{
    public void Configure(EntityTypeBuilder<BoletoBatchDTO> builder)
    {
        builder.ToTable("BoletoBatches");
        builder.HasKey(x => x.Id);
        builder.Property(x => x.Reference).IsRequired().HasMaxLength(80);
        builder.Property(x => x.UploadedByName).IsRequired().HasMaxLength(150);
        builder.Property(x => x.SourceFileName).IsRequired().HasMaxLength(260);
        builder.HasOne(x => x.License).WithMany().HasForeignKey(x => x.LicenseId).OnDelete(DeleteBehavior.Cascade);
        builder.HasIndex(x => new { x.LicenseId, x.Status, x.CreatedAt });
    }
}

public sealed class BoletoDocumentConfiguration : IEntityTypeConfiguration<BoletoDocumentDTO>
{
    public void Configure(EntityTypeBuilder<BoletoDocumentDTO> builder)
    {
        builder.ToTable("BoletoDocuments");
        builder.HasKey(x => x.Id);
        builder.Property(x => x.StorageReference).IsRequired().HasMaxLength(200);
        builder.Property(x => x.ExtractedSnippet).HasMaxLength(500);
        builder.HasOne(x => x.Batch).WithMany(x => x.Documents).HasForeignKey(x => x.BatchId).OnDelete(DeleteBehavior.Cascade);
        builder.HasOne(x => x.Unit).WithMany().HasForeignKey(x => x.UnitId).OnDelete(DeleteBehavior.SetNull);
        builder.HasIndex(x => x.BatchId);
        builder.HasIndex(x => x.UnitId);
    }
}
```

- [ ] **Step 3: Registrar no DbContext**

Create `CondotifyAPI.Infrastructure/DatabaseContext/Finance/DatabaseContext.Boleto.cs`:

```csharp
using CondotifyAPI.Domain.DTO.Finance;
using CondotifyAPI.Infrastructure.ContextConfiguration.Finance;
using Microsoft.EntityFrameworkCore;

namespace CondotifyAPI.Infrastructure;

public partial class DatabaseContext
{
    public DbSet<BoletoBatchDTO> BoletoBatches { get; set; }
    public DbSet<BoletoDocumentDTO> BoletoDocuments { get; set; }

    internal static void BoletoEntityConfiguration(ModelBuilder builder)
    {
        builder.ApplyConfiguration(new BoletoBatchConfiguration());
        builder.ApplyConfiguration(new BoletoDocumentConfiguration());
    }
}
```

In `CondotifyAPI.Infrastructure/DatabaseContext/DatabaseContext.cs`, add a line next to the other `*EntityConfiguration` calls inside `OnModelCreating`:

```csharp
        SafetyOperationsEntityConfiguration(modelBuilder);
        BoletoEntityConfiguration(modelBuilder);
```

- [ ] **Step 4: Gerar a migração**

Run: `dotnet ef migrations add AddBoletos --project CondotifyAPI.Infrastructure --startup-project CondotifyAPI`

Read the generated migration file in full before trusting it — confirm it only creates `BoletoBatches` and `BoletoDocuments` (with the FKs/indexes above) and touches nothing else. The dev database has real data; do not apply (`dotnet ef database update`) without confirming with whoever owns that database first — leave the migration generated but unapplied unless explicitly told to apply it.

- [ ] **Step 5: Build**

Run: `dotnet build CondotifyAPI.sln`
Expected: build limpo.

- [ ] **Step 6: Commit**

```bash
git add CondotifyAPI.Domain/DTO/Finance CondotifyAPI.Infrastructure/ContextConfiguration/Finance CondotifyAPI.Infrastructure/DatabaseContext/Finance CondotifyAPI.Infrastructure/DatabaseContext/DatabaseContext.cs CondotifyAPI.Infrastructure/Migrations
git commit -m "feat(boletos): add BoletoBatch/BoletoDocument entities and migration"
```

---

### Task 3: Armazenamento criptografado do PDF

**Files:**
- Create: `CondotifyAPI/Services/Finance/BoletoDocumentStore.cs`
- Modify: `CondotifyAPI/Program.cs`

**Interfaces:**
- Consumes: nada de tarefas anteriores.
- Produces: `IBoletoDocumentStore { Task<string> StoreAsync(Guid licenseId, byte[] pdfBytes, CancellationToken), Task<byte[]?> ReadAsync(Guid licenseId, string reference, CancellationToken), Task DeleteAsync(Guid licenseId, string? reference, CancellationToken) }` — usado pelas tarefas 6, 7 e 8.

- [ ] **Step 1: Implementar o serviço**

Create `CondotifyAPI/Services/Finance/BoletoDocumentStore.cs`:

```csharp
using System.Security.Cryptography;
using System.Text;

namespace CondotifyAPI.Services.Finance;

public interface IBoletoDocumentStore
{
    Task<string> StoreAsync(Guid licenseId, byte[] pdfBytes, CancellationToken cancellationToken = default);
    Task<byte[]?> ReadAsync(Guid licenseId, string reference, CancellationToken cancellationToken = default);
    Task DeleteAsync(Guid licenseId, string? reference, CancellationToken cancellationToken = default);
}

public sealed class BoletoDocumentStore : IBoletoDocumentStore
{
    private const int NonceSize = 12;
    private const int TagSize = 16;
    private const int MaxPageBytes = 2_000_000;
    private readonly string _root;
    private readonly byte[] _key;

    public BoletoDocumentStore(IConfiguration configuration)
    {
        _root = Environment.GetEnvironmentVariable("CONDOTIFY_BOLETO_STORAGE_PATH")
            ?? configuration["BoletoStorage:Path"]
            ?? Path.Combine(AppContext.BaseDirectory, "App_Data", "boletos");
        var secret = Environment.GetEnvironmentVariable("CONDOTIFY_MEDIA_SECRET")
            ?? Environment.GetEnvironmentVariable("CONDOTIFY_EQUIPMENT_SECRET")
            ?? throw new InvalidOperationException("Defina CONDOTIFY_MEDIA_SECRET para proteger os boletos.");
        _key = SHA256.HashData(Encoding.UTF8.GetBytes(secret));
        Directory.CreateDirectory(_root);
    }

    public async Task<string> StoreAsync(Guid licenseId, byte[] pdfBytes, CancellationToken cancellationToken = default)
    {
        if (pdfBytes.Length is < 1 or > MaxPageBytes) throw new InvalidOperationException("A pagina do boleto deve ter no maximo 2 MB.");
        var documentId = Guid.NewGuid();
        var directory = LicenseDirectory(licenseId);
        Directory.CreateDirectory(directory);
        var nonce = RandomNumberGenerator.GetBytes(NonceSize);
        var tag = new byte[TagSize];
        var cipher = new byte[pdfBytes.Length];
        using (var aes = new AesGcm(_key, TagSize)) aes.Encrypt(nonce, pdfBytes, cipher, tag);
        var payload = new byte[NonceSize + TagSize + cipher.Length];
        Buffer.BlockCopy(nonce, 0, payload, 0, NonceSize);
        Buffer.BlockCopy(tag, 0, payload, NonceSize, TagSize);
        Buffer.BlockCopy(cipher, 0, payload, NonceSize + TagSize, cipher.Length);
        await File.WriteAllBytesAsync(FilePath(licenseId, documentId), payload, cancellationToken);
        return Reference(licenseId, documentId);
    }

    public async Task<byte[]?> ReadAsync(Guid licenseId, string reference, CancellationToken cancellationToken = default)
    {
        if (!TryDocumentId(reference, out var documentId)) return null;
        var path = FilePath(licenseId, documentId);
        if (!File.Exists(path)) return null;
        var payload = await File.ReadAllBytesAsync(path, cancellationToken);
        if (payload.Length < NonceSize + TagSize) return null;
        var plain = new byte[payload.Length - NonceSize - TagSize];
        try
        {
            using var aes = new AesGcm(_key, TagSize);
            aes.Decrypt(
                payload.AsSpan(0, NonceSize),
                payload.AsSpan(NonceSize + TagSize),
                payload.AsSpan(NonceSize, TagSize),
                plain);
            return plain;
        }
        catch (CryptographicException)
        {
            CryptographicOperations.ZeroMemory(plain);
            return null;
        }
    }

    public Task DeleteAsync(Guid licenseId, string? reference, CancellationToken cancellationToken = default)
    {
        if (TryDocumentId(reference, out var documentId))
        {
            var path = FilePath(licenseId, documentId);
            if (File.Exists(path)) File.Delete(path);
        }
        return Task.CompletedTask;
    }

    private string LicenseDirectory(Guid licenseId) => Path.Combine(_root, licenseId.ToString("N"));
    private string FilePath(Guid licenseId, Guid documentId) => Path.Combine(LicenseDirectory(licenseId), $"{documentId:N}.bin");
    private static string Reference(Guid licenseId, Guid documentId) => $"/boletos-media/{licenseId:D}/{documentId:D}";

    private static bool TryDocumentId(string? reference, out Guid documentId)
    {
        documentId = Guid.Empty;
        if (string.IsNullOrWhiteSpace(reference)) return false;
        return Guid.TryParse(reference.TrimEnd('/').Split('/').LastOrDefault(), out documentId);
    }
}
```

- [ ] **Step 2: Registrar no DI**

Em `CondotifyAPI/Program.cs`, junto das outras linhas de storage (`builder.Services.AddSingleton<IPrivateMediaStore, PrivateMediaStore>();`), adicionar:

```csharp
builder.Services.AddSingleton<IBoletoDocumentStore, BoletoDocumentStore>();
```

(adicionar `using CondotifyAPI.Services.Finance;` no topo do arquivo se ainda não existir um `using` que cubra o namespace).

- [ ] **Step 3: Build**

Run: `dotnet build CondotifyAPI.sln`
Expected: build limpo.

- [ ] **Step 4: Commit**

```bash
git add CondotifyAPI/Services/Finance/BoletoDocumentStore.cs CondotifyAPI/Program.cs
git commit -m "feat(boletos): add encrypted per-page PDF storage"
```

---

### Task 4: Algoritmo de casamento (puro, testável)

**Files:**
- Create: `CondotifyAPI/Services/Finance/BoletoPageMatcher.cs`
- Create: `CondotifyAPI.Tests/BoletoPageMatcherTests.cs`

**Interfaces:**
- Consumes: `BoletoMatchMethodEnum` (Task 2).
- Produces: `BoletoPageMatcher.Match(string pageText, IReadOnlyCollection<BoletoPageMatcher.ResidentCandidate>, IReadOnlyCollection<BoletoPageMatcher.UnitCandidate>) : BoletoPageMatcher.MatchResult` — usado pela Tarefa 6.

- [ ] **Step 1: Escrever os testes primeiro**

Create `CondotifyAPI.Tests/BoletoPageMatcherTests.cs`:

```csharp
using CondotifyAPI.Domain.DTO.Finance;
using CondotifyAPI.Services.Finance;

namespace CondotifyAPI.Tests;

public sealed class BoletoPageMatcherTests
{
    private static readonly Guid UnitA = Guid.Parse("11111111-1111-1111-1111-111111111111");
    private static readonly Guid UnitB = Guid.Parse("22222222-2222-2222-2222-222222222222");

    private static readonly List<BoletoPageMatcher.ResidentCandidate> OneResident =
    [
        new(Guid.NewGuid(), "12345678901", UnitA)
    ];

    private static readonly List<BoletoPageMatcher.UnitCandidate> Units =
    [
        new(UnitA, "Bloco A", "101"),
        new(UnitB, "Bloco A", "102")
    ];

    [Fact]
    public void Match_ByCpf_WithPunctuation_Matches()
    {
        var result = BoletoPageMatcher.Match("Sacado: Maria Silva CPF: 123.456.789-01", OneResident, Units);

        Assert.Equal(UnitA, result.UnitId);
        Assert.Equal(BoletoMatchMethodEnum.Cpf, result.Method);
    }

    [Fact]
    public void Match_ByCpf_WithoutPunctuation_Matches()
    {
        var result = BoletoPageMatcher.Match("Sacado: Maria Silva CPF: 12345678901", OneResident, Units);

        Assert.Equal(UnitA, result.UnitId);
        Assert.Equal(BoletoMatchMethodEnum.Cpf, result.Method);
    }

    [Fact]
    public void Match_WithTwoDifferentCandidateCpfsOnSamePage_FallsBackToUnitText()
    {
        var residents = new List<BoletoPageMatcher.ResidentCandidate>
        {
            new(Guid.NewGuid(), "12345678901", UnitA),
            new(Guid.NewGuid(), "98765432100", UnitB)
        };
        var text = "CPF 123.456.789-01 CPF 987.654.321-00 Apto 101 Bloco A";

        var result = BoletoPageMatcher.Match(text, residents, Units);

        Assert.Equal(UnitA, result.UnitId);
        Assert.Equal(BoletoMatchMethodEnum.UnitText, result.Method);
    }

    [Theory]
    [InlineData("Unidade: Apto 101 - Bloco A")]
    [InlineData("Unidade 101 Bloco A")]
    [InlineData("Ap. 101 - Bloco A")]
    public void Match_ByUnitText_VariousPhrasings_Matches(string text)
    {
        var result = BoletoPageMatcher.Match(text, [], Units);

        Assert.Equal(UnitA, result.UnitId);
        Assert.Equal(BoletoMatchMethodEnum.UnitText, result.Method);
    }

    [Fact]
    public void Match_UnitNumberWithoutBlock_StillMatchesWhenUnambiguous()
    {
        var soleUnit = new List<BoletoPageMatcher.UnitCandidate> { new(UnitA, "Bloco A", "101") };

        var result = BoletoPageMatcher.Match("Apto 101", [], soleUnit);

        Assert.Equal(UnitA, result.UnitId);
        Assert.Equal(BoletoMatchMethodEnum.UnitText, result.Method);
    }

    [Fact]
    public void Match_SameUnitNumberInDifferentBlocks_DisambiguatesByBlockText()
    {
        var overlapping = new List<BoletoPageMatcher.UnitCandidate>
        {
            new(UnitA, "Bloco A", "101"),
            new(UnitB, "Bloco B", "101")
        };

        var result = BoletoPageMatcher.Match("Unidade 101 Bloco B", [], overlapping);

        Assert.Equal(UnitB, result.UnitId);
        Assert.Equal(BoletoMatchMethodEnum.UnitText, result.Method);
    }

    [Fact]
    public void Match_SameUnitNumberInDifferentBlocks_NoBlockText_ReturnsUnmatched()
    {
        var overlapping = new List<BoletoPageMatcher.UnitCandidate>
        {
            new(UnitA, "Bloco A", "101"),
            new(UnitB, "Bloco B", "101")
        };

        var result = BoletoPageMatcher.Match("Unidade 101", [], overlapping);

        Assert.Null(result.UnitId);
        Assert.Equal(BoletoMatchMethodEnum.Unmatched, result.Method);
    }

    [Fact]
    public void Match_NoCpfAndNoUnitText_ReturnsUnmatched()
    {
        var result = BoletoPageMatcher.Match("Folha de capa sem identificacao", OneResident, Units);

        Assert.Null(result.UnitId);
        Assert.Equal(BoletoMatchMethodEnum.Unmatched, result.Method);
    }

    [Fact]
    public void Match_EmptyText_ReturnsUnmatched()
    {
        var result = BoletoPageMatcher.Match(string.Empty, OneResident, Units);

        Assert.Null(result.UnitId);
        Assert.Equal(BoletoMatchMethodEnum.Unmatched, result.Method);
    }

    [Fact]
    public void Match_CpfPresentButUnknown_FallsBackToUnitText()
    {
        var text = "CPF 111.222.333-44 Apto 102 Bloco A";

        var result = BoletoPageMatcher.Match(text, OneResident, Units);

        Assert.Equal(UnitB, result.UnitId);
        Assert.Equal(BoletoMatchMethodEnum.UnitText, result.Method);
    }
}
```

- [ ] **Step 2: Rodar os testes e confirmar que falham**

Run: `dotnet test CondotifyAPI.Tests --filter BoletoPageMatcherTests`
Expected: FAIL (tipo `BoletoPageMatcher` ainda não existe).

- [ ] **Step 3: Implementar o matcher**

Create `CondotifyAPI/Services/Finance/BoletoPageMatcher.cs`:

```csharp
using System.Globalization;
using System.Text.RegularExpressions;
using CondotifyAPI.Domain.DTO.Finance;

namespace CondotifyAPI.Services.Finance;

// CPF e texto de unidade sao criterios de reforco, nunca escolha por
// adivinhacao: qualquer ambiguidade (mais de uma unidade candidata) cai
// direto para o proximo criterio, e se nenhum criterio resolver sozinho a
// pagina fica Unmatched para revisao manual do sindico.
public static class BoletoPageMatcher
{
    public sealed record ResidentCandidate(Guid ResidentId, string Cpf, Guid UnitId);
    public sealed record UnitCandidate(Guid UnitId, string BlockName, string UnitNumber);
    public readonly record struct MatchResult(Guid? UnitId, BoletoMatchMethodEnum Method);

    private static readonly Regex CpfPattern = new(@"\d{3}\.?\d{3}\.?\d{3}-?\d{2}", RegexOptions.Compiled);
    private static readonly Regex WhitespacePattern = new(@"\s+", RegexOptions.Compiled);

    public static MatchResult Match(
        string pageText,
        IReadOnlyCollection<ResidentCandidate> residents,
        IReadOnlyCollection<UnitCandidate> units)
    {
        var byCpf = MatchByCpf(pageText, residents);
        if (byCpf.UnitId is not null) return byCpf;

        return MatchByUnitText(pageText, units);
    }

    internal static MatchResult MatchByCpf(string pageText, IReadOnlyCollection<ResidentCandidate> residents)
    {
        if (string.IsNullOrWhiteSpace(pageText) || residents.Count == 0)
            return new MatchResult(null, BoletoMatchMethodEnum.Unmatched);

        var cpfsFound = ExtractCpfs(pageText).ToHashSet();
        if (cpfsFound.Count == 0)
            return new MatchResult(null, BoletoMatchMethodEnum.Unmatched);

        var matchedUnits = residents
            .Where(x => cpfsFound.Contains(x.Cpf))
            .Select(x => x.UnitId)
            .Distinct()
            .ToList();

        return matchedUnits.Count == 1
            ? new MatchResult(matchedUnits[0], BoletoMatchMethodEnum.Cpf)
            : new MatchResult(null, BoletoMatchMethodEnum.Unmatched);
    }

    internal static MatchResult MatchByUnitText(string pageText, IReadOnlyCollection<UnitCandidate> units)
    {
        if (string.IsNullOrWhiteSpace(pageText) || units.Count == 0)
            return new MatchResult(null, BoletoMatchMethodEnum.Unmatched);

        var normalized = Normalize(pageText);
        var numberMatches = units
            .Where(unit => HasUnitNumberReference(normalized, unit.UnitNumber))
            .ToList();
        if (numberMatches.Count == 0)
            return new MatchResult(null, BoletoMatchMethodEnum.Unmatched);

        // O numero sozinho ja basta quando so existe uma unidade candidata com
        // esse numero (a maioria dos boletos nao repete "Bloco X"). So exige o
        // nome do bloco no texto para desempatar quando o numero por si so e
        // ambiguo entre unidades de blocos diferentes.
        var byNumber = numberMatches.Select(unit => unit.UnitId).Distinct().ToList();
        if (byNumber.Count == 1)
            return new MatchResult(byNumber[0], BoletoMatchMethodEnum.UnitText);

        var byNumberAndBlock = numberMatches
            .Where(unit => !string.IsNullOrWhiteSpace(Normalize(unit.BlockName)) &&
                           normalized.Contains(Normalize(unit.BlockName), StringComparison.Ordinal))
            .Select(unit => unit.UnitId)
            .Distinct()
            .ToList();

        return byNumberAndBlock.Count == 1
            ? new MatchResult(byNumberAndBlock[0], BoletoMatchMethodEnum.UnitText)
            : new MatchResult(null, BoletoMatchMethodEnum.Unmatched);
    }

    internal static IEnumerable<string> ExtractCpfs(string text) =>
        CpfPattern.Matches(text)
            .Select(match => new string(match.Value.Where(char.IsDigit).ToArray()))
            .Where(digits => digits.Length == 11);

    internal static bool HasUnitNumberReference(string normalizedText, string unitNumber)
    {
        var unitKey = Normalize(unitNumber);
        if (string.IsNullOrWhiteSpace(unitKey)) return false;

        var unitPattern = $@"\b(apto|apartamento|unidade|ap)\.?\s*{Regex.Escape(unitKey)}\b";
        return Regex.IsMatch(normalizedText, unitPattern);
    }

    internal static string Normalize(string value)
    {
        var decomposed = value.Normalize(NormalizationForm.FormD);
        var stripped = new string(decomposed.Where(c => CharUnicodeInfo.GetUnicodeCategory(c) != UnicodeCategory.NonSpacingMark).ToArray());
        return WhitespacePattern.Replace(stripped, " ").Trim().ToLowerInvariant();
    }
}
```

- [ ] **Step 4: Rodar os testes e confirmar que passam**

Run: `dotnet test CondotifyAPI.Tests --filter BoletoPageMatcherTests`
Expected: PASS (9 testes).

- [ ] **Step 5: Commit**

```bash
git add CondotifyAPI/Services/Finance/BoletoPageMatcher.cs CondotifyAPI.Tests/BoletoPageMatcherTests.cs
git commit -m "feat(boletos): add pure CPF/unit-text matching algorithm with tests"
```

---

### Task 5: Processamento de PDF (extração de texto e divisão em páginas)

**Files:**
- Create: `CondotifyAPI/Services/Finance/BoletoPdfProcessor.cs`
- Modify: `CondotifyAPI/CondotifyAPI.csproj`
- Modify: `CondotifyAPI/Program.cs`

**Interfaces:**
- Produces: `IBoletoPdfProcessor { int CountPages(byte[] pdf), string ExtractPageText(byte[] pdf, int pageNumber), byte[] ExtractPageAsPdf(byte[] pdf, int pageNumber) }` (`pageNumber` é 1-based) — usado pela Tarefa 6.

- [ ] **Step 1: Adicionar as bibliotecas**

Run (a partir da raiz do repositório):
```bash
dotnet add CondotifyAPI/CondotifyAPI.csproj package UglyToad.PdfPig
dotnet add CondotifyAPI/CondotifyAPI.csproj package PdfSharp
```

- [ ] **Step 2: Implementar o processador**

Create `CondotifyAPI/Services/Finance/BoletoPdfProcessor.cs`:

```csharp
using PdfSharp.Pdf;
using PdfSharp.Pdf.IO;
using UglyToad.PdfPig;

namespace CondotifyAPI.Services.Finance;

public interface IBoletoPdfProcessor
{
    int CountPages(byte[] pdfBytes);
    string ExtractPageText(byte[] pdfBytes, int pageNumber);
    byte[] ExtractPageAsPdf(byte[] pdfBytes, int pageNumber);
}

public sealed class BoletoPdfProcessor : IBoletoPdfProcessor
{
    public int CountPages(byte[] pdfBytes)
    {
        using var document = PdfDocument.Open(pdfBytes);
        return document.NumberOfPages;
    }

    public string ExtractPageText(byte[] pdfBytes, int pageNumber)
    {
        using var document = PdfDocument.Open(pdfBytes);
        return document.GetPage(pageNumber).Text;
    }

    public byte[] ExtractPageAsPdf(byte[] pdfBytes, int pageNumber)
    {
        using var input = new MemoryStream(pdfBytes);
        using var source = PdfReader.Open(input, PdfDocumentOpenMode.Import);
        using var output = new PdfDocument();
        output.AddPage(source.Pages[pageNumber - 1]);
        using var buffer = new MemoryStream();
        output.Save(buffer, closeStream: false);
        return buffer.ToArray();
    }
}
```

- [ ] **Step 3: Registrar no DI**

Em `CondotifyAPI/Program.cs`, junto de `IBoletoDocumentStore`:

```csharp
builder.Services.AddSingleton<IBoletoPdfProcessor, BoletoPdfProcessor>();
```

- [ ] **Step 4: Build**

Run: `dotnet build CondotifyAPI.sln`
Expected: build limpo. Se `PdfReader.Open`/`PdfDocument.Open` reportarem overload diferente do esperado, ajustar para a assinatura real restaurada (a API pública de ambas as bibliotecas é estável nesse ponto — leitura/escrita de PDF a partir de `Stream`/`byte[]`).

- [ ] **Step 5: Commit**

```bash
git add CondotifyAPI/Services/Finance/BoletoPdfProcessor.cs CondotifyAPI/CondotifyAPI.csproj CondotifyAPI/Program.cs
git commit -m "feat(boletos): add PDF text extraction and page-splitting"
```

---

### Task 6: Controller de staff — upload, listagem e detalhe do lote

**Files:**
- Create: `CondotifyAPI/Data/Finance/BoletoDtos.cs`
- Create: `CondotifyAPI/Controllers/BoletosController.cs`
- Create: `CondotifyAPI.Tests/BoletosControllerTests.cs`

**Interfaces:**
- Consumes: `IBoletoDocumentStore` (Tarefa 3), `IBoletoPdfProcessor` (Tarefa 5), `BoletoPageMatcher` (Tarefa 4), `BoletoBatchDTO`/`BoletoDocumentDTO` (Tarefa 2), `LicensePermissionEnum.ManageFinance` (Tarefa 1).
- Produces: rotas `POST/GET api/access/licenses/{licenseId:guid}/boletos/batches`, `GET api/access/licenses/{licenseId:guid}/boletos/batches/{batchId:guid}`, `GET api/access/licenses/{licenseId:guid}/boletos/documents/{documentId:guid}/file` — consumidas pela Tarefa 9. `BoletoBatchOut`/`BoletoDocumentOut`/`BoletoBatchDetailOut` são consumidos também pela Tarefa 7 (mesmo controller).

- [ ] **Step 1: Contratos de request/response**

Create `CondotifyAPI/Data/Finance/BoletoDtos.cs`:

```csharp
namespace CondotifyAPI.Data.Finance;

public sealed class BoletoBatchOut
{
    public Guid Id { get; set; }
    public string Reference { get; set; } = string.Empty;
    public DateTime DueDate { get; set; }
    public string Status { get; set; } = string.Empty;
    public int TotalPages { get; set; }
    public int MatchedCount { get; set; }
    public int UnmatchedCount { get; set; }
    public string UploadedByName { get; set; } = string.Empty;
    public DateTime CreatedAt { get; set; }
    public DateTime? PublishedAt { get; set; }
}

public sealed class BoletoDocumentOut
{
    public Guid Id { get; set; }
    public int PageNumber { get; set; }
    public Guid? UnitId { get; set; }
    public string UnitLabel { get; set; } = string.Empty;
    public string MatchMethod { get; set; } = string.Empty;
    public bool Ignored { get; set; }
    public string ExtractedSnippet { get; set; } = string.Empty;
}

public sealed class BoletoBatchDetailOut
{
    public BoletoBatchOut Batch { get; set; } = new();
    public List<BoletoDocumentOut> Documents { get; set; } = [];
}

public sealed class BoletoBatchUploadForm
{
    public string Reference { get; set; } = string.Empty;
    public DateTime DueDate { get; set; }
    public IFormFile File { get; set; } = null!;
}

public sealed class BoletoDocumentUpdateIn
{
    public Guid? UnitId { get; set; }
    public bool Ignored { get; set; }
}
```

- [ ] **Step 2: Controller — upload, listagem, detalhe**

Create `CondotifyAPI/Controllers/BoletosController.cs`:

```csharp
using System.Security.Claims;
using CondotifyAPI.Data.Finance;
using CondotifyAPI.Domain.DTO.Finance;
using CondotifyAPI.Infrastructure;
using CondotifyAPI.Services.Authorization;
using CondotifyAPI.Services.Finance;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Microsoft.EntityFrameworkCore;

namespace CondotifyAPI.Controllers;

[ApiController]
[Authorize]
[Route("api/access/licenses/{licenseId:guid}/boletos")]
public sealed class BoletosController(
    DatabaseContext context,
    IBoletoDocumentStore store,
    IBoletoPdfProcessor pdf) : ControllerBase
{
    private const int MaxPages = 600;

    [HttpPost("batches")]
    [RequireLicensePermission(LicensePermissionEnum.ManageFinance)]
    [RequestSizeLimit(20_000_000)]
    public async Task<IActionResult> UploadBatch(Guid licenseId, [FromForm] BoletoBatchUploadForm form, CancellationToken cancellationToken)
    {
        if (string.IsNullOrWhiteSpace(form.Reference) || form.Reference.Length > 80)
            return BadRequest(new { Result = "InvalidReference", Errors = "Informe uma referencia valida para o lote." });
        if (form.File is null || form.File.Length == 0)
            return BadRequest(new { Result = "FileRequired", Errors = "Selecione o PDF com os boletos." });
        if (!string.Equals(form.File.ContentType, "application/pdf", StringComparison.OrdinalIgnoreCase))
            return BadRequest(new { Result = "InvalidFileType", Errors = "O arquivo deve ser um PDF." });

        await using var stream = new MemoryStream();
        await form.File.CopyToAsync(stream, cancellationToken);
        var sourceBytes = stream.ToArray();

        int pageCount;
        try
        {
            pageCount = pdf.CountPages(sourceBytes);
        }
        catch (Exception)
        {
            return BadRequest(new { Result = "InvalidPdf", Errors = "Nao foi possivel ler o PDF enviado." });
        }
        if (pageCount == 0 || pageCount > MaxPages)
            return BadRequest(new { Result = "InvalidPageCount", Errors = $"O PDF deve ter entre 1 e {MaxPages} paginas." });

        var actorId = Guid.TryParse(User.FindFirstValue(ClaimTypes.NameIdentifier), out var parsedActor) ? parsedActor : Guid.Empty;
        var actorName = User.FindFirstValue("name") ?? User.Identity?.Name ?? "Administracao";
        var now = DateTime.UtcNow;

        var batch = new BoletoBatchDTO
        {
            Id = Guid.NewGuid(),
            LicenseId = licenseId,
            Reference = form.Reference.Trim(),
            DueDate = form.DueDate,
            UploadedByUserId = actorId,
            UploadedByName = actorName,
            Status = BoletoBatchStatusEnum.Processing,
            SourceFileName = Path.GetFileName(form.File.FileName),
            TotalPages = pageCount,
            CreatedAt = now
        };
        context.BoletoBatches.Add(batch);

        // Um candidato por morador: se ele tiver mais de um vinculo ativo nesta
        // licenca, usa o vinculo principal (IsPrimary) - nunca gera mais de uma
        // unidade candidata para o mesmo CPF por conta propria.
        var residents = (await context.Residents.AsNoTracking()
            .Where(x => x.CPF != "" && x.UnitLinks.Any(link => link.IsActive && link.Unit.Block.LicenseId == licenseId))
            .Select(x => new
            {
                x.Id,
                x.CPF,
                UnitId = x.UnitLinks
                    .Where(link => link.IsActive && link.Unit.Block.LicenseId == licenseId)
                    .OrderByDescending(link => link.IsPrimary)
                    .Select(link => link.UnitId)
                    .First()
            })
            .ToListAsync(cancellationToken))
            .Select(x => new BoletoPageMatcher.ResidentCandidate(x.Id, x.CPF, x.UnitId))
            .ToList();

        var units = (await context.Units.AsNoTracking()
            .Where(x => x.Block.LicenseId == licenseId)
            .Select(x => new { x.Id, BlockName = x.Block.Name, x.Number })
            .ToListAsync(cancellationToken))
            .Select(x => new BoletoPageMatcher.UnitCandidate(x.Id, x.BlockName, x.Number))
            .ToList();

        for (var pageNumber = 1; pageNumber <= pageCount; pageNumber++)
        {
            var pageText = SafeExtractText(sourceBytes, pageNumber);
            var match = BoletoPageMatcher.Match(pageText, residents, units);
            var pagePdf = pdf.ExtractPageAsPdf(sourceBytes, pageNumber);
            var reference = await store.StoreAsync(licenseId, pagePdf, cancellationToken);

            context.BoletoDocuments.Add(new BoletoDocumentDTO
            {
                Id = Guid.NewGuid(),
                BatchId = batch.Id,
                UnitId = match.UnitId,
                PageNumber = pageNumber,
                MatchMethod = match.Method,
                Ignored = false,
                StorageReference = reference,
                ExtractedSnippet = Snippet(pageText),
                CreatedAt = now
            });
        }

        batch.Status = BoletoBatchStatusEnum.PendingReview;
        await context.SaveChangesAsync(cancellationToken);

        var detail = await LoadDetailAsync(licenseId, batch.Id, cancellationToken);
        return detail is null ? NotFound() : Ok(detail);
    }

    [HttpGet("batches")]
    [RequireLicensePermission(LicensePermissionEnum.ManageFinance)]
    public async Task<IActionResult> ListBatches(Guid licenseId, CancellationToken cancellationToken)
    {
        var batches = await context.BoletoBatches.AsNoTracking()
            .Include(x => x.Documents)
            .Where(x => x.LicenseId == licenseId)
            .OrderByDescending(x => x.CreatedAt)
            .ToListAsync(cancellationToken);

        return Ok(batches.Select(x => ToBatchOut(x, x.Documents)).ToList());
    }

    [HttpGet("batches/{batchId:guid}")]
    [RequireLicensePermission(LicensePermissionEnum.ManageFinance)]
    public async Task<IActionResult> GetBatch(Guid licenseId, Guid batchId, CancellationToken cancellationToken)
    {
        var detail = await LoadDetailAsync(licenseId, batchId, cancellationToken);
        return detail is null ? NotFound() : Ok(detail);
    }

    [HttpGet("documents/{documentId:guid}/file")]
    [RequireLicensePermission(LicensePermissionEnum.ManageFinance)]
    public async Task<IActionResult> GetDocumentFile(Guid licenseId, Guid documentId, CancellationToken cancellationToken)
    {
        var document = await context.BoletoDocuments.AsNoTracking()
            .FirstOrDefaultAsync(x => x.Id == documentId && x.Batch.LicenseId == licenseId, cancellationToken);
        if (document is null) return NotFound();

        var bytes = await store.ReadAsync(licenseId, document.StorageReference, cancellationToken);
        return bytes is null ? NotFound() : File(bytes, "application/pdf");
    }

    private async Task<BoletoBatchDetailOut?> LoadDetailAsync(Guid licenseId, Guid batchId, CancellationToken cancellationToken)
    {
        var batch = await context.BoletoBatches.AsNoTracking()
            .Include(x => x.Documents).ThenInclude(x => x.Unit).ThenInclude(x => x!.Block)
            .FirstOrDefaultAsync(x => x.Id == batchId && x.LicenseId == licenseId, cancellationToken);
        if (batch is null) return null;

        return new BoletoBatchDetailOut
        {
            Batch = ToBatchOut(batch, batch.Documents),
            Documents = batch.Documents
                .OrderBy(x => x.PageNumber)
                .Select(ToDocumentOut)
                .ToList()
        };
    }

    private static BoletoBatchOut ToBatchOut(BoletoBatchDTO batch, ICollection<BoletoDocumentDTO> documents) => new()
    {
        Id = batch.Id,
        Reference = batch.Reference,
        DueDate = batch.DueDate,
        Status = batch.Status.ToString(),
        TotalPages = batch.TotalPages,
        MatchedCount = documents.Count(x => x.UnitId.HasValue),
        UnmatchedCount = documents.Count(x => !x.UnitId.HasValue && !x.Ignored),
        UploadedByName = batch.UploadedByName,
        CreatedAt = batch.CreatedAt,
        PublishedAt = batch.PublishedAt
    };

    private static BoletoDocumentOut ToDocumentOut(BoletoDocumentDTO document) => new()
    {
        Id = document.Id,
        PageNumber = document.PageNumber,
        UnitId = document.UnitId,
        UnitLabel = document.Unit is null ? string.Empty : $"{document.Unit.Block.Name} / {document.Unit.Number}".Trim(' ', '/'),
        MatchMethod = document.MatchMethod.ToString(),
        Ignored = document.Ignored,
        ExtractedSnippet = document.ExtractedSnippet
    };

    private string SafeExtractText(byte[] sourceBytes, int pageNumber)
    {
        try { return pdf.ExtractPageText(sourceBytes, pageNumber); }
        catch (Exception) { return string.Empty; }
    }

    private static string Snippet(string pageText)
    {
        var collapsed = string.Join(' ', pageText.Split((char[]?)null, StringSplitOptions.RemoveEmptyEntries));
        return collapsed.Length > 240 ? collapsed[..240] : collapsed;
    }
}
```

- [ ] **Step 3: Testes por reflexão**

Create `CondotifyAPI.Tests/BoletosControllerTests.cs`:

```csharp
using CondotifyAPI.Controllers;
using CondotifyAPI.Services.Authorization;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.Routing;

namespace CondotifyAPI.Tests;

public sealed class BoletosControllerTests
{
    [Fact]
    public void Controller_RequiresAuthorization()
    {
        var authorize = typeof(BoletosController).GetCustomAttributes(typeof(AuthorizeAttribute), inherit: true);
        Assert.Single(authorize);
    }

    [Theory]
    [InlineData(nameof(BoletosController.UploadBatch), typeof(HttpPostAttribute), "batches")]
    [InlineData(nameof(BoletosController.ListBatches), typeof(HttpGetAttribute), "batches")]
    [InlineData(nameof(BoletosController.GetBatch), typeof(HttpGetAttribute), "batches/{batchId:guid}")]
    [InlineData(nameof(BoletosController.GetDocumentFile), typeof(HttpGetAttribute), "documents/{documentId:guid}/file")]
    public void Actions_UseExpectedRouteAndVerb(string actionName, Type httpAttributeType, string route)
    {
        var method = typeof(BoletosController).GetMethod(actionName);

        Assert.NotNull(method);
        var httpAttribute = Assert.IsAssignableFrom<HttpMethodAttribute>(
            method!.GetCustomAttributes(httpAttributeType, inherit: true).Single());
        Assert.Equal(route, httpAttribute.Template);

        var permission = Assert.Single(method.GetCustomAttributes<RequireLicensePermissionAttribute>(inherit: true));
        Assert.Equal(LicensePermissionEnum.ManageFinance, Assert.IsType<LicensePermissionEnum>(Assert.Single(permission.Arguments!)));
    }
}
```

- [ ] **Step 4: Build e testes**

Run: `dotnet build CondotifyAPI.sln && dotnet test CondotifyAPI.Tests --filter "BoletosControllerTests|BoletoPageMatcherTests"`
Expected: build limpo, todos os testes passam.

- [ ] **Step 5: Commit**

```bash
git add CondotifyAPI/Data/Finance CondotifyAPI/Controllers/BoletosController.cs CondotifyAPI.Tests/BoletosControllerTests.cs
git commit -m "feat(boletos): add staff upload/list/detail endpoints with automatic matching"
```

---

### Task 7: Controller de staff — revisão, publicação, cancelamento, exclusão

**Files:**
- Modify: `CondotifyAPI/Data/Finance/BoletoDtos.cs`
- Modify: `CondotifyAPI/Controllers/BoletosController.cs`
- Modify: `CondotifyAPI.Tests/BoletosControllerTests.cs`

**Interfaces:**
- Consumes: `BoletosController` (Tarefa 6, mesmo arquivo/classe parcial), `IPlatformPushNotifier.NotifyResidentAsync` (já existe em `CondotifyAPI.Services.Mobile`).
- Produces: rotas `PUT api/.../documents/{documentId:guid}`, `POST api/.../batches/{batchId:guid}/publish`, `POST api/.../batches/{batchId:guid}/cancel`, `DELETE api/.../documents/{documentId:guid}` — consumidas pela Tarefa 10.

- [ ] **Step 1: Contrato de publicação**

Em `CondotifyAPI/Data/Finance/BoletoDtos.cs`, adicionar ao final:

```csharp
public sealed class BoletoPublishResultOut
{
    public int PublishedCount { get; set; }
    public int IgnoredCount { get; set; }
}
```

- [ ] **Step 2: Endpoints de revisão/publicação**

Em `CondotifyAPI/Controllers/BoletosController.cs`:

1. Adicionar aos usings: `using CondotifyAPI.Domain.Enums.Mobile;` e `using CondotifyAPI.Services.Mobile;`.
2. Trocar o construtor primário:

```csharp
public sealed class BoletosController(
    DatabaseContext context,
    IBoletoDocumentStore store,
    IBoletoPdfProcessor pdf,
    IPlatformPushNotifier notifier) : ControllerBase
```

3. Adicionar os métodos abaixo (após `GetDocumentFile`):

```csharp
    [HttpPut("documents/{documentId:guid}")]
    [RequireLicensePermission(LicensePermissionEnum.ManageFinance)]
    public async Task<IActionResult> UpdateDocument(Guid licenseId, Guid documentId, [FromBody] BoletoDocumentUpdateIn input, CancellationToken cancellationToken)
    {
        var document = await context.BoletoDocuments
            .Include(x => x.Batch)
            .FirstOrDefaultAsync(x => x.Id == documentId && x.Batch.LicenseId == licenseId, cancellationToken);
        if (document is null) return NotFound();
        if (document.Batch.Status != BoletoBatchStatusEnum.PendingReview)
            return Conflict(new { Result = "BatchNotEditable", Errors = "So e possivel editar um lote em revisao." });

        if (input.UnitId.HasValue)
        {
            var unitExists = await context.Units.AsNoTracking().AnyAsync(x => x.Id == input.UnitId && x.Block.LicenseId == licenseId, cancellationToken);
            if (!unitExists) return BadRequest(new { Result = "InvalidUnit", Errors = "Unidade invalida para esta licenca." });
        }

        document.UnitId = input.Ignored ? null : input.UnitId;
        document.Ignored = input.Ignored;
        if (input.UnitId.HasValue && !input.Ignored) document.MatchMethod = BoletoMatchMethodEnum.Manual;
        await context.SaveChangesAsync(cancellationToken);

        var detail = await LoadDetailAsync(licenseId, document.BatchId, cancellationToken);
        return detail is null ? NotFound() : Ok(detail);
    }

    [HttpPost("batches/{batchId:guid}/publish")]
    [RequireLicensePermission(LicensePermissionEnum.ManageFinance)]
    public async Task<IActionResult> Publish(Guid licenseId, Guid batchId, CancellationToken cancellationToken)
    {
        var batch = await context.BoletoBatches
            .Include(x => x.Documents).ThenInclude(x => x.Unit).ThenInclude(x => x!.ResidentLinks)
            .FirstOrDefaultAsync(x => x.Id == batchId && x.LicenseId == licenseId, cancellationToken);
        if (batch is null) return NotFound();
        if (batch.Status != BoletoBatchStatusEnum.PendingReview)
            return Conflict(new { Result = "BatchNotReady", Errors = "Este lote nao esta aguardando revisao." });

        var pending = batch.Documents.Where(x => !x.Ignored && !x.UnitId.HasValue).ToList();
        if (pending.Count > 0)
            return UnprocessableEntity(new { Result = "PendingPages", Errors = $"{pending.Count} pagina(s) ainda sem unidade definida." });

        var toPublish = batch.Documents.Where(x => !x.Ignored).ToList();
        var duplicateUnits = toPublish
            .GroupBy(x => x.UnitId!.Value)
            .Where(group => group.Count() > 1)
            .Select(group => group.Key)
            .ToList();
        if (duplicateUnits.Count > 0)
            return UnprocessableEntity(new { Result = "DuplicateUnits", Errors = $"{duplicateUnits.Count} unidade(s) com mais de um boleto neste lote. Corrija antes de publicar." });

        var toIgnore = batch.Documents.Where(x => x.Ignored).ToList();
        foreach (var document in toIgnore)
        {
            await store.DeleteAsync(licenseId, document.StorageReference, cancellationToken);
            context.BoletoDocuments.Remove(document);
        }

        batch.Status = BoletoBatchStatusEnum.Published;
        batch.PublishedAt = DateTime.UtcNow;
        await context.SaveChangesAsync(cancellationToken);

        foreach (var document in toPublish)
        {
            var residentIds = document.Unit?.ResidentLinks
                .Where(link => link.IsActive)
                .Select(link => link.ResidentId)
                .Distinct() ?? [];
            foreach (var residentId in residentIds)
            {
                await notifier.NotifyResidentAsync(
                    residentId,
                    MobileNotificationCategory.Financial,
                    "Novo boleto disponivel",
                    $"Seu boleto de {batch.Reference} ja esta disponivel.",
                    "/boletos",
                    $"boleto-published:{document.Id:N}",
                    cancellationToken);
            }
        }

        return Ok(new BoletoPublishResultOut { PublishedCount = toPublish.Count, IgnoredCount = toIgnore.Count });
    }

    [HttpPost("batches/{batchId:guid}/cancel")]
    [RequireLicensePermission(LicensePermissionEnum.ManageFinance)]
    public async Task<IActionResult> Cancel(Guid licenseId, Guid batchId, CancellationToken cancellationToken)
    {
        var batch = await context.BoletoBatches
            .Include(x => x.Documents)
            .FirstOrDefaultAsync(x => x.Id == batchId && x.LicenseId == licenseId, cancellationToken);
        if (batch is null) return NotFound();
        if (batch.Status != BoletoBatchStatusEnum.PendingReview)
            return Conflict(new { Result = "BatchNotCancellable", Errors = "So e possivel cancelar um lote em revisao." });

        foreach (var document in batch.Documents)
            await store.DeleteAsync(licenseId, document.StorageReference, cancellationToken);

        context.BoletoBatches.Remove(batch);
        await context.SaveChangesAsync(cancellationToken);
        return Ok(new { Result = "Cancelled" });
    }

    [HttpDelete("documents/{documentId:guid}")]
    [RequireLicensePermission(LicensePermissionEnum.ManageFinance)]
    public async Task<IActionResult> DeleteDocument(Guid licenseId, Guid documentId, CancellationToken cancellationToken)
    {
        var document = await context.BoletoDocuments
            .Include(x => x.Batch)
            .FirstOrDefaultAsync(x => x.Id == documentId && x.Batch.LicenseId == licenseId, cancellationToken);
        if (document is null) return NotFound();
        if (document.Batch.Status != BoletoBatchStatusEnum.Published)
            return Conflict(new { Result = "BatchNotPublished", Errors = "Use cancelar o lote para remover paginas antes da publicacao." });

        await store.DeleteAsync(licenseId, document.StorageReference, cancellationToken);
        context.BoletoDocuments.Remove(document);
        await context.SaveChangesAsync(cancellationToken);
        return Ok(new { Result = "Deleted" });
    }
```

- [ ] **Step 3: Estender os testes por reflexão**

Em `CondotifyAPI.Tests/BoletosControllerTests.cs`, adicionar ao `[Theory]` existente (mesmo atributo `[InlineData]` de rota/verbo/permissão, sem precisar de outro teste):

```csharp
    [InlineData(nameof(BoletosController.UpdateDocument), typeof(HttpPutAttribute), "documents/{documentId:guid}")]
    [InlineData(nameof(BoletosController.Publish), typeof(HttpPostAttribute), "batches/{batchId:guid}/publish")]
    [InlineData(nameof(BoletosController.Cancel), typeof(HttpPostAttribute), "batches/{batchId:guid}/cancel")]
    [InlineData(nameof(BoletosController.DeleteDocument), typeof(HttpDeleteAttribute), "documents/{documentId:guid}")]
```

(essas linhas entram junto das `[InlineData]` já existentes, acima do método `Actions_UseExpectedRouteAndVerb` — o mesmo teste parametrizado cobre as rotas novas).

- [ ] **Step 4: Build e testes**

Run: `dotnet build CondotifyAPI.sln && dotnet test CondotifyAPI.Tests --filter BoletosControllerTests`
Expected: build limpo, todos os testes passam.

- [ ] **Step 5: Commit**

```bash
git add CondotifyAPI/Data/Finance/BoletoDtos.cs CondotifyAPI/Controllers/BoletosController.cs CondotifyAPI.Tests/BoletosControllerTests.cs
git commit -m "feat(boletos): add review, publish, cancel and delete endpoints with resident notification"
```

---

### Task 8: Controller do morador — listagem e download

**Files:**
- Create: `CondotifyAPI/Controllers/ResidentBoletosController.cs`
- Create: `CondotifyAPI.Tests/ResidentBoletosControllerTests.cs`

**Interfaces:**
- Consumes: `IResidentAuthorizationService.GetGrantAsync` (já existe, `ResidentAccessGrant.UnitIds`), `IBoletoDocumentStore` (Tarefa 3), `BoletoBatchDTO`/`BoletoDocumentDTO` (Tarefa 2).
- Produces: rotas `GET api/resident/boletos`, `GET api/resident/boletos/{documentId:guid}/file` — consumidas pela Tarefa 11. `ResidentBoletoOut` é consumido pela Tarefa 11.

- [ ] **Step 1: Contrato de saída**

Create `CondotifyAPI/Data/Finance/ResidentBoletoDtos.cs`:

```csharp
namespace CondotifyAPI.Data.Finance;

public sealed class ResidentBoletoOut
{
    public Guid DocumentId { get; set; }
    public string Reference { get; set; } = string.Empty;
    public DateTime DueDate { get; set; }
    public string UnitLabel { get; set; } = string.Empty;
    public DateTime PublishedAt { get; set; }
}
```

- [ ] **Step 2: Controller**

Create `CondotifyAPI/Controllers/ResidentBoletosController.cs`:

```csharp
using CondotifyAPI.Data.Finance;
using CondotifyAPI.Domain.DTO.Finance;
using CondotifyAPI.Infrastructure;
using CondotifyAPI.Services.Authorization;
using CondotifyAPI.Services.Finance;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Microsoft.EntityFrameworkCore;

namespace CondotifyAPI.Controllers;

[ApiController]
[Authorize(Policy = "Resident")]
[Route("api/resident/boletos")]
public sealed class ResidentBoletosController(
    DatabaseContext context,
    IResidentAuthorizationService authorization,
    IBoletoDocumentStore store) : ControllerBase
{
    [HttpGet]
    public async Task<IActionResult> List(CancellationToken cancellationToken)
    {
        var grant = await authorization.GetGrantAsync(User, cancellationToken);
        if (grant is null) return Forbid();
        if (grant.UnitIds.Count == 0) return Ok(Array.Empty<ResidentBoletoOut>());

        var documents = await context.BoletoDocuments.AsNoTracking()
            .Include(x => x.Batch)
            .Include(x => x.Unit).ThenInclude(x => x!.Block)
            .Where(x => x.Batch.Status == BoletoBatchStatusEnum.Published &&
                        x.UnitId.HasValue && grant.UnitIds.Contains(x.UnitId.Value))
            .OrderByDescending(x => x.Batch.PublishedAt)
            .Select(x => new ResidentBoletoOut
            {
                DocumentId = x.Id,
                Reference = x.Batch.Reference,
                DueDate = x.Batch.DueDate,
                UnitLabel = (x.Unit!.Block.Name + " / " + x.Unit.Number).Trim(),
                PublishedAt = x.Batch.PublishedAt!.Value
            })
            .ToListAsync(cancellationToken);

        return Ok(documents);
    }

    [HttpGet("{documentId:guid}/file")]
    public async Task<IActionResult> Download(Guid documentId, CancellationToken cancellationToken)
    {
        var grant = await authorization.GetGrantAsync(User, cancellationToken);
        if (grant is null) return Forbid();

        var document = await context.BoletoDocuments.AsNoTracking()
            .Include(x => x.Batch)
            .FirstOrDefaultAsync(x =>
                x.Id == documentId &&
                x.Batch.Status == BoletoBatchStatusEnum.Published &&
                x.Batch.LicenseId == grant.LicenseId, cancellationToken);
        if (document is null || !document.UnitId.HasValue || !grant.UnitIds.Contains(document.UnitId.Value))
            return NotFound();

        var bytes = await store.ReadAsync(grant.LicenseId, document.StorageReference, cancellationToken);
        return bytes is null ? NotFound() : File(bytes, "application/pdf");
    }
}
```

- [ ] **Step 3: Testes por reflexão**

Create `CondotifyAPI.Tests/ResidentBoletosControllerTests.cs`:

```csharp
using CondotifyAPI.Controllers;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.Routing;

namespace CondotifyAPI.Tests;

public sealed class ResidentBoletosControllerTests
{
    [Fact]
    public void Controller_RequiresTheResidentPolicy()
    {
        var authorize = Assert.Single(typeof(ResidentBoletosController)
            .GetCustomAttributes(typeof(AuthorizeAttribute), inherit: true)
            .Cast<AuthorizeAttribute>());

        Assert.Equal("Resident", authorize.Policy);
    }

    [Theory]
    [InlineData(nameof(ResidentBoletosController.List), typeof(HttpGetAttribute), null)]
    [InlineData(nameof(ResidentBoletosController.Download), typeof(HttpGetAttribute), "{documentId:guid}/file")]
    public void Actions_UseExpectedRouteAndVerb(string actionName, Type httpAttributeType, string? route)
    {
        var method = typeof(ResidentBoletosController).GetMethod(actionName);

        Assert.NotNull(method);
        var httpAttribute = Assert.IsAssignableFrom<HttpMethodAttribute>(
            method!.GetCustomAttributes(httpAttributeType, inherit: true).Single());
        Assert.Equal(route, httpAttribute.Template);
        Assert.Empty(method.GetCustomAttributes(typeof(AllowAnonymousAttribute), inherit: true));
        Assert.Empty(method.GetCustomAttributes(typeof(AuthorizeAttribute), inherit: true));
    }
}
```

- [ ] **Step 4: Build e testes**

Run: `dotnet build CondotifyAPI.sln && dotnet test CondotifyAPI.Tests --filter ResidentBoletosControllerTests`
Expected: build limpo, todos os testes passam.

- [ ] **Step 5: Commit**

```bash
git add CondotifyAPI/Data/Finance/ResidentBoletoDtos.cs CondotifyAPI/Controllers/ResidentBoletosController.cs CondotifyAPI.Tests/ResidentBoletosControllerTests.cs
git commit -m "feat(boletos): add resident list/download endpoints scoped to active unit links"
```

---

### Task 9: Portal web — lista de lotes, explicativo animado e upload

**Files:**
- Create: `Condotify.Contracts/FinanceViewModels.cs`
- Modify: `Condotify.ApiClient/CondotifyApiClient.cs`
- Create: `Condotify/Components/Dialogs/BoletoUploadDialog.razor`
- Create: `Condotify/Components/LicenseModules/BoletosModule.razor`
- Modify: `Condotify/Components/Pages/LicenseWorkspace.razor`
- Modify: `Condotify/wwwroot/css/portal.css`

**Interfaces:**
- Consumes: `POST/GET api/access/licenses/{licenseId}/boletos/batches`, `GET .../batches/{batchId}` (Tarefa 6).
- Produces: `BoletoBatchViewModel`, `BoletoDocumentViewModel`, `BoletoBatchDetailViewModel` (Contracts) e os métodos `UploadBoletoBatchAsync`/`GetBoletoBatchesAsync`/`GetBoletoBatchAsync` do `CondotifyApiClient` — consumidos pela Tarefa 10. `BoletosModule` é aberto pela Tarefa 10 (`BoletoReviewDialog`).

- [ ] **Step 1: ViewModels compartilhados**

Create `Condotify.Contracts/FinanceViewModels.cs`:

```csharp
namespace Condotify.Contracts;

public sealed class BoletoBatchViewModel
{
    public Guid Id { get; set; }
    public string Reference { get; set; } = string.Empty;
    public DateTime DueDate { get; set; }
    public string Status { get; set; } = string.Empty;
    public int TotalPages { get; set; }
    public int MatchedCount { get; set; }
    public int UnmatchedCount { get; set; }
    public string UploadedByName { get; set; } = string.Empty;
    public DateTime CreatedAt { get; set; }
    public DateTime? PublishedAt { get; set; }
}

public sealed class BoletoDocumentViewModel
{
    public Guid Id { get; set; }
    public int PageNumber { get; set; }
    public Guid? UnitId { get; set; }
    public string UnitLabel { get; set; } = string.Empty;
    public string MatchMethod { get; set; } = string.Empty;
    public bool Ignored { get; set; }
    public string ExtractedSnippet { get; set; } = string.Empty;
}

public sealed class BoletoBatchDetailViewModel
{
    public BoletoBatchViewModel Batch { get; set; } = new();
    public List<BoletoDocumentViewModel> Documents { get; set; } = [];
}

public sealed class BoletoPublishResultViewModel
{
    public int PublishedCount { get; set; }
    public int IgnoredCount { get; set; }
}

public sealed class ResidentBoletoViewModel
{
    public Guid DocumentId { get; set; }
    public string Reference { get; set; } = string.Empty;
    public DateTime DueDate { get; set; }
    public string UnitLabel { get; set; } = string.Empty;
    public DateTime PublishedAt { get; set; }
}
```

- [ ] **Step 2: Métodos no CondotifyApiClient**

Em `Condotify.ApiClient/CondotifyApiClient.cs`, adicionar (perto dos outros métodos de licença, por exemplo depois de `GetStructureAsync`):

```csharp
    public Task<ApiResult<List<BoletoBatchViewModel>>> GetBoletoBatchesAsync(Guid licenseId, CancellationToken cancellationToken = default) =>
        GetAsync<List<BoletoBatchViewModel>>($"api/access/licenses/{licenseId}/boletos/batches", cancellationToken);

    public Task<ApiResult<BoletoBatchDetailViewModel>> GetBoletoBatchAsync(Guid licenseId, Guid batchId, CancellationToken cancellationToken = default) =>
        GetAsync<BoletoBatchDetailViewModel>($"api/access/licenses/{licenseId}/boletos/batches/{batchId}", cancellationToken);

    public async Task<ApiResult<BoletoBatchDetailViewModel>> UploadBoletoBatchAsync(
        Guid licenseId,
        string reference,
        DateTime dueDate,
        string fileName,
        byte[] fileBytes,
        CancellationToken cancellationToken = default)
    {
        try
        {
            using var client = await CreateClientAsync(cancellationToken);
            using var content = new MultipartFormDataContent
            {
                { new StringContent(reference), "Reference" },
                { new StringContent(dueDate.ToString("O")), "DueDate" }
            };
            var fileContent = new ByteArrayContent(fileBytes);
            fileContent.Headers.ContentType = new System.Net.Http.Headers.MediaTypeHeaderValue("application/pdf");
            content.Add(fileContent, "File", fileName);

            using var response = await client.PostAsync(BuildApiUrl($"api/access/licenses/{licenseId}/boletos/batches"), content, cancellationToken);
            if (!response.IsSuccessStatusCode)
                return ApiResult<BoletoBatchDetailViewModel>.Fail(await ReadErrorAsync(response, "Nao foi possivel enviar o lote de boletos."), response.StatusCode);

            var value = await response.Content.ReadFromJsonAsync<BoletoBatchDetailViewModel>(GetJsonOptions, cancellationToken);
            return value is null
                ? ApiResult<BoletoBatchDetailViewModel>.Fail("Nao foi possivel interpretar a resposta da API.", response.StatusCode)
                : ApiResult<BoletoBatchDetailViewModel>.Ok(value, response.StatusCode);
        }
        catch (OperationCanceledException) when (cancellationToken.IsCancellationRequested)
        {
            return ApiResult<BoletoBatchDetailViewModel>.Fail("Operação cancelada.");
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Falha ao enviar lote de boletos para a licenca {LicenseId}", licenseId);
            return ApiResult<BoletoBatchDetailViewModel>.Fail("A API está indisponível. Tente novamente em instantes.");
        }
    }
```

(Confirmar se `GetJsonOptions` e `ReadErrorAsync` já estão acessíveis nesse ponto do arquivo — ambos são `private static` na mesma classe parcial, então sim.)

- [ ] **Step 3: Dialog de upload**

Create `Condotify/Components/Dialogs/BoletoUploadDialog.razor`:

```razor
@inject CondotifyApiClient Api

<MudDialog Class="entity-dialog">
    <DialogContent>
        <div class="dialog-description">
            <MudIcon Icon="@Icons.Material.Outlined.UploadFile" />
            <div>
                <strong>Novo lote de boletos</strong>
                <span>Envie o PDF único exportado pela administradora — uma cobrança por página.</span>
            </div>
        </div>

        @if (!string.IsNullOrWhiteSpace(_error))
        {
            <MudAlert Severity="Severity.Error" Variant="Variant.Outlined" Class="mb-4">@_error</MudAlert>
        }

        <MudTextField T="string" Label="Referência" Placeholder="Agosto/2026" @bind-Value="_reference" Class="mb-3" MaxLength="80" />
        <MudDatePicker Label="Vencimento" @bind-Date="_dueDate" Class="mb-3" />

        <div class="import-source">
            <div class="import-source-copy">
                <MudIcon Icon="@Icons.Material.Outlined.PictureAsPdf" />
                <div>
                    <strong>@(string.IsNullOrWhiteSpace(_fileName) ? "Selecione o PDF" : _fileName)</strong>
                    <span>Limite de 20 MB.</span>
                </div>
            </div>
            <label class="file-button @(_loading ? "disabled" : null)">
                <MudIcon Icon="@Icons.Material.Outlined.UploadFile" />
                <span>@(_fileBytes is null ? "Selecionar PDF" : "Trocar arquivo")</span>
                <InputFile OnChange="ReadFileAsync" accept=".pdf,application/pdf" disabled="@_loading" />
            </label>
        </div>
    </DialogContent>
    <DialogActions>
        <MudButton Variant="Variant.Text" Disabled="_loading" OnClick="Cancel">Cancelar</MudButton>
        <MudButton Variant="Variant.Filled" Color="Color.Primary" StartIcon="@Icons.Material.Outlined.CloudUpload"
                   Disabled="@(!CanSubmit || _loading)" OnClick="SubmitAsync">
            @(_loading ? "Enviando..." : "Enviar e processar")
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] private IMudDialogInstance Dialog { get; set; } = null!;
    [Parameter, EditorRequired] public Guid LicenseId { get; set; }

    private const long MaxFileSize = 20_000_000;
    private string _reference = string.Empty;
    private DateTime? _dueDate = DateTime.Today.AddDays(10);
    private string _fileName = string.Empty;
    private byte[]? _fileBytes;
    private bool _loading;
    private string? _error;

    private bool CanSubmit => !string.IsNullOrWhiteSpace(_reference) && _dueDate.HasValue && _fileBytes is { Length: > 0 };

    private void Cancel() => Dialog.Cancel();

    private async Task ReadFileAsync(InputFileChangeEventArgs args)
    {
        _error = null;
        var file = args.File;
        if (file.Size > MaxFileSize)
        {
            _error = "O arquivo excede o limite de 20 MB.";
            return;
        }

        await using var stream = file.OpenReadStream(MaxFileSize);
        await using var buffer = new MemoryStream();
        await stream.CopyToAsync(buffer);
        _fileBytes = buffer.ToArray();
        _fileName = file.Name;
    }

    private async Task SubmitAsync()
    {
        if (!CanSubmit) return;
        _loading = true;
        _error = null;
        try
        {
            var result = await Api.UploadBoletoBatchAsync(LicenseId, _reference.Trim(), _dueDate!.Value, _fileName, _fileBytes!);
            if (!result.Success || result.Value is null)
            {
                _error = result.Error ?? "Não foi possível processar o lote.";
                return;
            }
            Dialog.Close(DialogResult.Ok(result.Value));
        }
        finally
        {
            _loading = false;
        }
    }
}
```

- [ ] **Step 4: Módulo — lista e explicativo animado**

Create `Condotify/Components/LicenseModules/BoletosModule.razor`:

```razor
@inject CondotifyApiClient Api
@inject IDialogService DialogService

@if (!string.IsNullOrWhiteSpace(_error)) { <MudAlert Severity="Severity.Error" Variant="Variant.Outlined" Class="mb-4">@_error</MudAlert> }

@if (_loading)
{
    <div class="loading-state"><MudProgressCircular Indeterminate Color="Color.Primary" /></div>
}
else if (_batches.Count == 0)
{
    <div class="boletos-onboard">
        <div class="boletos-onboard-track"><i></i></div>
        <div class="boletos-onboard-steps">
            <div class="boletos-onboard-step boletos-onboard-step-one">
                <span><MudIcon Icon="@Icons.Material.Outlined.UploadFile" /></span>
                <div><small>PASSO 1</small><strong>Envie o PDF</strong><span>O lote completo exportado da administradora, uma página por unidade.</span></div>
            </div>
            <div class="boletos-onboard-step boletos-onboard-step-two">
                <span><MudIcon Icon="@Icons.Material.Outlined.FactCheck" /></span>
                <div><small>PASSO 2</small><strong>Conferência automática</strong><span>O sistema já casa cada página com a unidade certa.</span></div>
            </div>
            <div class="boletos-onboard-step boletos-onboard-step-three">
                <span><MudIcon Icon="@Icons.Material.Outlined.Send" /></span>
                <div><small>PASSO 3</small><strong>Publique pros moradores</strong><span>Um clique libera tudo de uma vez.</span></div>
            </div>
        </div>
        <MudButton Variant="Variant.Filled" Color="Color.Primary" StartIcon="@Icons.Material.Outlined.UploadFile" Class="boletos-onboard-cta" OnClick="OpenUploadAsync" Disabled="!CanManage">
            Enviar primeiro lote
        </MudButton>
    </div>
}
else
{
    <section class="content-panel">
        <div class="panel-heading">
            <div><MudText Typo="Typo.h5">Lotes de boletos</MudText><MudText Typo="Typo.caption" Color="Color.Secondary">Histórico de envios para esta licença</MudText></div>
            <MudButton Variant="Variant.Filled" Color="Color.Primary" StartIcon="@Icons.Material.Outlined.UploadFile" OnClick="OpenUploadAsync" Disabled="!CanManage">Novo lote</MudButton>
        </div>
        <MudTable Items="_batches" Hover Dense Elevation="0">
            <HeaderContent>
                <MudTh>Referência</MudTh>
                <MudTh>Vencimento</MudTh>
                <MudTh>Status</MudTh>
                <MudTh>Páginas</MudTh>
                <MudTh>Enviado por</MudTh>
            </HeaderContent>
            <RowTemplate>
                <MudTd DataLabel="Referência">@context.Reference</MudTd>
                <MudTd DataLabel="Vencimento">@context.DueDate.ToString("dd/MM/yyyy")</MudTd>
                <MudTd DataLabel="Status"><MudChip T="string" Size="Size.Small" Color="@StatusColor(context.Status)" Variant="Variant.Outlined">@StatusLabel(context.Status)</MudChip></MudTd>
                <MudTd DataLabel="Páginas">@context.MatchedCount / @context.TotalPages casadas</MudTd>
                <MudTd DataLabel="Enviado por">@context.UploadedByName</MudTd>
            </RowTemplate>
        </MudTable>
    </section>
}

@code {
    [Parameter] public Guid LicenseId { get; set; }
    [Parameter] public bool CanManage { get; set; }

    private List<BoletoBatchViewModel> _batches = [];
    private bool _loading;
    private string? _error;

    protected override Task OnParametersSetAsync() => LoadAsync();

    private async Task LoadAsync()
    {
        _loading = true;
        _error = null;
        var result = await Api.GetBoletoBatchesAsync(LicenseId);
        _loading = false;
        if (result.Success) _batches = result.Value ?? [];
        else _error = result.Error;
    }

    private async Task OpenUploadAsync()
    {
        var parameters = new DialogParameters { [nameof(BoletoUploadDialog.LicenseId)] = LicenseId };
        var dialog = await DialogService.ShowAsync<BoletoUploadDialog>("Novo lote de boletos", parameters,
            new DialogOptions { CloseOnEscapeKey = true, MaxWidth = MaxWidth.Small, FullWidth = true });
        var result = await dialog.Result;
        if (result is { Canceled: false }) await LoadAsync();
    }

    private static string StatusLabel(string status) => status switch
    {
        "Processing" => "Processando",
        "PendingReview" => "Em revisão",
        "Published" => "Publicado",
        "Cancelled" => "Cancelado",
        _ => status
    };

    private static Color StatusColor(string status) => status switch
    {
        "PendingReview" => Color.Warning,
        "Published" => Color.Success,
        "Cancelled" => Color.Default,
        _ => Color.Info
    };
}
```

- [ ] **Step 5: Wiring no LicenseWorkspace**

Em `Condotify/Components/Pages/LicenseWorkspace.razor`:

1. No `<nav>`, logo após a linha do `agendamento`:
```razor
        @if (Has(LicensePermission.ManageFinance)) { @NavButton("boletos", "Boletos", Icons.Material.Outlined.ReceiptLong) }
```
2. No `@switch (CurrentSection)`, logo após o `case "agendamento":`:
```razor
            case "boletos":
                <BoletosModule LicenseId="LicenseId" CanManage="@Has(LicensePermission.ManageFinance)" />
                break;
```
3. Em `DefaultSection`, adicionar uma linha antes de `: Has(LicensePermission.ViewUsers) ...`:
```csharp
        : Has(LicensePermission.ManageFinance) ? "boletos"
```
4. Em `SectionAllowed`, adicionar antes do `"administracao" =>`:
```csharp
        "boletos" => Has(LicensePermission.ManageFinance),
```

- [ ] **Step 6: CSS**

Em `Condotify/wwwroot/css/portal.css`, adicionar ao final:

```css
.boletos-onboard { display: grid; grid-template-columns: 40px minmax(0, 1fr); gap: 0 16px; padding: 26px 24px; border-radius: 18px; background: linear-gradient(145deg, #f4f7ff, #ffffff); border: 1px solid #e7ebf2; }
.boletos-onboard-track { width: 2px; position: relative; margin: 10px 0; border-radius: 999px; background: #dbe4f2; overflow: hidden; }
.boletos-onboard-track i { width: 100%; height: 70px; position: absolute; top: -70px; left: 0; border-radius: inherit; background: linear-gradient(180deg, transparent, #3471e2, transparent); animation: invite-progress-run 6s ease-in-out infinite; }
.boletos-onboard-steps { display: grid; gap: 18px; }
.boletos-onboard-step { display: grid; grid-template-columns: 44px minmax(0, 1fr); gap: 14px; align-items: center; padding: 14px 16px; border: 1px solid #e7ebf2; border-radius: 14px; background: #fff; animation: invite-step-focus 6s ease-in-out infinite; }
.boletos-onboard-step > span { width: 44px; height: 44px; display: grid; place-items: center; border-radius: 12px; color: #3156d3; background: #eef1fb; }
.boletos-onboard-step strong { display: block; color: #263043; font-size: .95rem; }
.boletos-onboard-step small { color: #8994a4; font-size: .65rem; font-weight: 750; letter-spacing: .08em; }
.boletos-onboard-step span:last-child { color: #687386; font-size: .82rem; }
.boletos-onboard-step-two { animation-delay: 2s; }
.boletos-onboard-step-three { animation-delay: 4s; }
.boletos-onboard-cta { grid-column: 2; margin-top: 22px; justify-self: start; }
```

- [ ] **Step 7: Build**

Run: `dotnet build Condotify.sln`
Expected: build limpo. `BoletosModule.razor` ainda não abre nenhuma tela de revisão nesta tarefa — isso é adicionado na Tarefa 10, que edita este mesmo arquivo.

- [ ] **Step 8: Commit**

```bash
git add Condotify.Contracts/FinanceViewModels.cs Condotify.ApiClient/CondotifyApiClient.cs Condotify/Components/Dialogs/BoletoUploadDialog.razor Condotify/Components/LicenseModules/BoletosModule.razor Condotify/Components/Pages/LicenseWorkspace.razor Condotify/wwwroot/css/portal.css
git commit -m "feat(boletos): add web portal batch list, animated first-run guide and upload dialog"
```

---

### Task 10: Portal web — revisão e publicação

**Files:**
- Create: `Condotify/Components/Dialogs/BoletoReviewDialog.razor`
- Modify: `Condotify/Components/LicenseModules/BoletosModule.razor`
- Modify: `Condotify.ApiClient/CondotifyApiClient.cs`

**Interfaces:**
- Consumes: `GET .../batches/{batchId}`, `PUT .../documents/{documentId}`, `POST .../batches/{batchId}/publish`, `POST .../batches/{batchId}/cancel`, `DELETE .../documents/{documentId}` (Tarefa 7); `GetStructureAsync` (já existe) para o seletor de unidades; `BoletosModule.razor` (Tarefa 9, editado aqui para abrir o novo dialog).

- [ ] **Step 1: Métodos adicionais no CondotifyApiClient**

Em `Condotify.ApiClient/CondotifyApiClient.cs`, logo abaixo dos métodos adicionados na Tarefa 9:

```csharp
    public Task<ApiResult<BoletoBatchDetailViewModel>> UpdateBoletoDocumentAsync(Guid licenseId, Guid documentId, Guid? unitId, bool ignored, CancellationToken cancellationToken = default) =>
        SendForAsync<BoletoBatchDetailViewModel>(HttpMethod.Put, $"api/access/licenses/{licenseId}/boletos/documents/{documentId}", new { UnitId = unitId, Ignored = ignored }, cancellationToken);

    public Task<ApiResult<BoletoPublishResultViewModel>> PublishBoletoBatchAsync(Guid licenseId, Guid batchId, CancellationToken cancellationToken = default) =>
        SendForAsync<BoletoPublishResultViewModel>(HttpMethod.Post, $"api/access/licenses/{licenseId}/boletos/batches/{batchId}/publish", new { }, cancellationToken);

    public Task<ApiResult<bool>> CancelBoletoBatchAsync(Guid licenseId, Guid batchId, CancellationToken cancellationToken = default) =>
        DeleteViaPostAsync($"api/access/licenses/{licenseId}/boletos/batches/{batchId}/cancel", cancellationToken);

    public Task<ApiResult<bool>> DeleteBoletoDocumentAsync(Guid licenseId, Guid documentId, CancellationToken cancellationToken = default) =>
        DeleteAsync($"api/access/licenses/{licenseId}/boletos/documents/{documentId}", cancellationToken);

    public Task<ApiResult<string>> GetBoletoDocumentFileAsync(Guid licenseId, Guid documentId, CancellationToken cancellationToken = default) =>
        GetPdfDataUrlAsync($"api/access/licenses/{licenseId}/boletos/documents/{documentId}/file", cancellationToken);

    private async Task<ApiResult<string>> GetPdfDataUrlAsync(string path, CancellationToken cancellationToken)
    {
        try
        {
            using var client = await CreateClientAsync(cancellationToken);
            using var response = await client.GetAsync(BuildApiUrl(path), cancellationToken);
            if (!response.IsSuccessStatusCode)
                return ApiResult<string>.Fail(await ReadErrorAsync(response, "O arquivo esta indisponivel."), response.StatusCode);

            var mediaType = response.Content.Headers.ContentType?.MediaType;
            if (!string.Equals(mediaType, "application/pdf", StringComparison.OrdinalIgnoreCase))
                return ApiResult<string>.Fail("A API retornou um arquivo invalido.", response.StatusCode);

            var content = await response.Content.ReadAsByteArrayAsync(cancellationToken);
            return content.Length == 0
                ? ApiResult<string>.Fail("A API retornou um arquivo vazio.", response.StatusCode)
                : ApiResult<string>.Ok($"data:{mediaType};base64,{Convert.ToBase64String(content)}", response.StatusCode);
        }
        catch (OperationCanceledException) when (cancellationToken.IsCancellationRequested)
        {
            return ApiResult<string>.Fail("Operação cancelada.");
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Falha ao consultar arquivo em {ApiPath}", path);
            return ApiResult<string>.Fail("A API está indisponível. Tente novamente em instantes.");
        }
    }

    private async Task<ApiResult<bool>> DeleteViaPostAsync(string path, CancellationToken cancellationToken)
    {
        var response = await SendAsync(HttpMethod.Post, path, new { }, false, cancellationToken);
        response.Document?.Dispose();
        return response.Success
            ? ApiResult<bool>.Ok(true, response.StatusCode)
            : ApiResult<bool>.Fail(response.Error!, response.StatusCode);
    }
```

- [ ] **Step 2: Dialog de revisão**

Create `Condotify/Components/Dialogs/BoletoReviewDialog.razor`:

```razor
@inject CondotifyApiClient Api
@inject ISnackbar Snackbar
@inject IJSRuntime JS

<MudDialog Class="entity-dialog entity-dialog-wide">
    <DialogContent>
        @if (!string.IsNullOrWhiteSpace(_error)) { <MudAlert Severity="Severity.Error" Variant="Variant.Outlined" Class="mb-3">@_error</MudAlert> }

        @if (_loading)
        {
            <div class="loading-state"><MudProgressCircular Indeterminate Color="Color.Primary" /></div>
        }
        else if (_detail is not null)
        {
            <div class="dialog-description mb-3">
                <MudIcon Icon="@Icons.Material.Outlined.FactCheck" />
                <div>
                    <strong>@_detail.Batch.Reference</strong>
                    <span>@_detail.Batch.MatchedCount de @_detail.Batch.TotalPages páginas casadas automaticamente. Corrija o que faltar antes de publicar.</span>
                </div>
            </div>

            @if (!string.IsNullOrWhiteSpace(_actionError))
            {
                <MudAlert Severity="Severity.Error" Variant="Variant.Outlined" Class="mb-3">@_actionError</MudAlert>
            }

            <MudTable Items="_detail.Documents" Hover Dense Elevation="0" Class="import-preview-table">
                <HeaderContent>
                    <MudTh>Página</MudTh>
                    <MudTh>Trecho identificado</MudTh>
                    <MudTh>Unidade</MudTh>
                    <MudTh></MudTh>
                </HeaderContent>
                <RowTemplate>
                    <MudTd DataLabel="Página">
                        <MudLink Disabled="@(_opening == context.Id)" OnClick="@(() => OpenPageAsync(context.Id))">#@context.PageNumber</MudLink>
                    </MudTd>
                    <MudTd DataLabel="Trecho"><small class="audit-summary">@context.ExtractedSnippet</small></MudTd>
                    <MudTd DataLabel="Unidade">
                        @if (context.Ignored)
                        {
                            <MudChip T="string" Size="Size.Small" Color="Color.Default" Variant="Variant.Outlined">Ignorada</MudChip>
                        }
                        else
                        {
                            <MudSelect T="Guid?" Value="context.UnitId" ValueChanged="@(value => AssignUnitAsync(context, value))" Dense Placeholder="Selecionar unidade...">
                                @foreach (var unit in _units)
                                {
                                    <MudSelectItem T="Guid?" Value="unit.Id">@unit.Label</MudSelectItem>
                                }
                            </MudSelect>
                        }
                    </MudTd>
                    <MudTd>
                        <MudTooltip Text="@(context.Ignored ? "Restaurar página" : "Ignorar página (capa/lixo)")">
                            <MudIconButton Icon="@(context.Ignored ? Icons.Material.Outlined.RestoreFromTrash : Icons.Material.Outlined.Block)"
                                           Size="Size.Small" OnClick="@(() => ToggleIgnoreAsync(context))" />
                        </MudTooltip>
                    </MudTd>
                </RowTemplate>
            </MudTable>
        }
    </DialogContent>
    <DialogActions>
        @if (_detail?.Batch.Status == "PendingReview")
        {
            <MudButton Variant="Variant.Text" Color="Color.Error" Disabled="_busy" OnClick="CancelBatchAsync">Cancelar lote</MudButton>
            <MudSpacer />
            <MudButton Variant="Variant.Text" Disabled="_busy" OnClick="Close">Fechar</MudButton>
            <MudButton Variant="Variant.Filled" Color="Color.Primary" StartIcon="@Icons.Material.Outlined.Send"
                       Disabled="@(!CanPublish || _busy)" OnClick="PublishAsync">
                @(_busy ? "Publicando..." : "Publicar lote")
            </MudButton>
        }
        else
        {
            <MudButton Variant="Variant.Text" OnClick="Close">Fechar</MudButton>
        }
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] private IMudDialogInstance Dialog { get; set; } = null!;
    [Parameter, EditorRequired] public Guid LicenseId { get; set; }
    [Parameter, EditorRequired] public Guid BatchId { get; set; }

    private BoletoBatchDetailViewModel? _detail;
    private List<(Guid Id, string Label)> _units = [];
    private bool _loading;
    private bool _busy;
    private Guid? _opening;
    private string? _error;
    private string? _actionError;

    private bool CanPublish => _detail is not null &&
        _detail.Documents.Where(x => !x.Ignored).All(x => x.UnitId.HasValue);

    protected override Task OnInitializedAsync() => LoadAsync();

    private async Task OpenPageAsync(Guid documentId)
    {
        _opening = documentId;
        var result = await Api.GetBoletoDocumentFileAsync(LicenseId, documentId);
        _opening = null;
        if (!result.Success || string.IsNullOrWhiteSpace(result.Value))
        {
            Snackbar.Add(result.Error ?? "Não foi possível abrir a página.", Severity.Error);
            return;
        }
        await JS.InvokeVoidAsync("open", result.Value, "_blank");
    }

    private async Task LoadAsync()
    {
        _loading = true;
        _error = null;
        var detailTask = Api.GetBoletoBatchAsync(LicenseId, BatchId);
        var structureTask = Api.GetStructureAsync(LicenseId);
        await Task.WhenAll(detailTask, structureTask);
        _loading = false;

        var detail = await detailTask;
        if (detail.Success) _detail = detail.Value;
        else _error = detail.Error;

        var structure = await structureTask;
        if (structure.Success && structure.Value is not null)
        {
            _units = structure.Value.Blocks
                .SelectMany(block => block.Units.Select(unit => (unit.Id, Label: $"{block.Name} / {unit.Number}")))
                .OrderBy(x => x.Label)
                .ToList();
        }
    }

    private async Task AssignUnitAsync(BoletoDocumentViewModel document, Guid? unitId)
    {
        _actionError = null;
        var result = await Api.UpdateBoletoDocumentAsync(LicenseId, document.Id, unitId, false);
        if (!result.Success || result.Value is null) { _actionError = result.Error; return; }
        _detail = result.Value;
    }

    private async Task ToggleIgnoreAsync(BoletoDocumentViewModel document)
    {
        _actionError = null;
        var result = await Api.UpdateBoletoDocumentAsync(LicenseId, document.Id, document.Ignored ? document.UnitId : null, !document.Ignored);
        if (!result.Success || result.Value is null) { _actionError = result.Error; return; }
        _detail = result.Value;
    }

    private async Task PublishAsync()
    {
        if (!CanPublish) return;
        _busy = true;
        _actionError = null;
        try
        {
            var result = await Api.PublishBoletoBatchAsync(LicenseId, BatchId);
            if (!result.Success)
            {
                _actionError = result.Error;
                return;
            }
            Snackbar.Add("Lote publicado. Os moradores já podem ver seus boletos.", Severity.Success);
            Dialog.Close(DialogResult.Ok(true));
        }
        finally
        {
            _busy = false;
        }
    }

    private async Task CancelBatchAsync()
    {
        _busy = true;
        _actionError = null;
        try
        {
            var result = await Api.CancelBoletoBatchAsync(LicenseId, BatchId);
            if (!result.Success)
            {
                _actionError = result.Error;
                return;
            }
            Snackbar.Add("Lote cancelado.", Severity.Info);
            Dialog.Close(DialogResult.Ok(true));
        }
        finally
        {
            _busy = false;
        }
    }

    private void Close() => Dialog.Close(DialogResult.Ok(false));
}
```

- [ ] **Step 3: Ligar o dialog ao módulo de lista**

Em `Condotify/Components/LicenseModules/BoletosModule.razor` (criado na Tarefa 9):

1. No `<HeaderContent>` da `MudTable`, devolver a coluna de ação — trocar:
```razor
                    <MudTh>Enviado por</MudTh>
                </HeaderContent>
```
por:
```razor
                    <MudTh>Enviado por</MudTh>
                    <MudTh></MudTh>
                </HeaderContent>
```

2. No `<RowTemplate>`, devolver o botão de ação — trocar:
```razor
                    <MudTd DataLabel="Enviado por">@context.UploadedByName</MudTd>
                </RowTemplate>
```
por:
```razor
                    <MudTd DataLabel="Enviado por">@context.UploadedByName</MudTd>
                    <MudTd>
                        <MudButton Variant="Variant.Text" Size="Size.Small" OnClick="() => OpenReviewAsync(context.Id)" Disabled="!CanManage">
                            @(context.Status == "PendingReview" ? "Revisar" : "Detalhes")
                        </MudButton>
                    </MudTd>
                </RowTemplate>
```

3. No bloco `@code`, adicionar o método que abre o dialog (logo após `OpenUploadAsync`):
```csharp
    private async Task OpenReviewAsync(Guid batchId)
    {
        var parameters = new DialogParameters { [nameof(BoletoReviewDialog.LicenseId)] = LicenseId, [nameof(BoletoReviewDialog.BatchId)] = batchId };
        var dialog = await DialogService.ShowAsync<BoletoReviewDialog>("Revisão do lote", parameters,
            new DialogOptions { CloseOnEscapeKey = true, MaxWidth = MaxWidth.Large, FullWidth = true });
        var result = await dialog.Result;
        if (result is { Canceled: false }) await LoadAsync();
    }
```

4. Abrir a revisão automaticamente depois de um upload bem-sucedido — o método inteiro `OpenUploadAsync` (não `OpenReviewAsync`, que fica como está) passa de:
```csharp
    private async Task OpenUploadAsync()
    {
        var parameters = new DialogParameters { [nameof(BoletoUploadDialog.LicenseId)] = LicenseId };
        var dialog = await DialogService.ShowAsync<BoletoUploadDialog>("Novo lote de boletos", parameters,
            new DialogOptions { CloseOnEscapeKey = true, MaxWidth = MaxWidth.Small, FullWidth = true });
        var result = await dialog.Result;
        if (result is { Canceled: false }) await LoadAsync();
    }
```
para:
```csharp
    private async Task OpenUploadAsync()
    {
        var parameters = new DialogParameters { [nameof(BoletoUploadDialog.LicenseId)] = LicenseId };
        var dialog = await DialogService.ShowAsync<BoletoUploadDialog>("Novo lote de boletos", parameters,
            new DialogOptions { CloseOnEscapeKey = true, MaxWidth = MaxWidth.Small, FullWidth = true });
        var result = await dialog.Result;
        if (result is { Canceled: false })
        {
            await LoadAsync();
            if (result.Data is BoletoBatchDetailViewModel detail)
                await OpenReviewAsync(detail.Batch.Id);
        }
    }
```

- [ ] **Step 4: Build**

Run: `dotnet build Condotify.sln`
Expected: build limpo. Nenhum CSS novo é necessário — `BoletoReviewDialog` reaproveita `.entity-dialog`, `.entity-dialog-wide`, `.dialog-description` e `.import-preview-table`, já existentes em `portal.css` (a última já vem com `max-height`/`overflow: auto`).

- [ ] **Step 5: Commit**

```bash
git add Condotify/Components/Dialogs/BoletoReviewDialog.razor Condotify/Components/LicenseModules/BoletosModule.razor Condotify.ApiClient/CondotifyApiClient.cs
git commit -m "feat(boletos): add web portal review and publish dialog"
```

---

### Task 11: App mobile — visão do morador

**Files:**
- Modify: `Condotify.ApiClient/CondotifyApiClient.cs`
- Create: `Condotify.Mobile/Components/Pages/Boletos.razor`
- Modify: `Condotify.Mobile/Components/Pages/More.razor`
- Modify: `Condotify.Mobile/Components/Pages/Notifications.razor`
- Modify: `Condotify.Mobile/wwwroot/css/app.css`

**Interfaces:**
- Consumes: `GET api/resident/boletos`, `GET api/resident/boletos/{documentId}/file` (Tarefa 8); `ResidentBoletoViewModel` (Tarefa 9); `MobileNotificationCategory.Financial` (Tarefa 1); helper privado `GetPdfDataUrlAsync` (já existe em `CondotifyApiClient`, adicionado pela Tarefa 10 — não recriar).

- [ ] **Step 1: Métodos no CondotifyApiClient**

Em `Condotify.ApiClient/CondotifyApiClient.cs`, junto dos outros métodos `api/resident/...` — reaproveita o helper privado `GetPdfDataUrlAsync` que a Tarefa 10 já adicionou nesta mesma classe (não declarar de novo, só chamar):

```csharp
    public Task<ApiResult<List<ResidentBoletoViewModel>>> GetResidentBoletosAsync(CancellationToken cancellationToken = default) =>
        GetAsync<List<ResidentBoletoViewModel>>("api/resident/boletos", cancellationToken);

    public Task<ApiResult<string>> GetResidentBoletoFileAsync(Guid documentId, CancellationToken cancellationToken = default) =>
        GetPdfDataUrlAsync($"api/resident/boletos/{documentId}/file", cancellationToken);
```

- [ ] **Step 2: Página do morador**

Create `Condotify.Mobile/Components/Pages/Boletos.razor`:

```razor
@page "/boletos"
@inject CondotifyApiClient Api
@inject IJSRuntime JS
@inject ISnackbar Snackbar

<PageTitle>Boletos | Condotify</PageTitle>
<PageHeader Eyebrow="FINANCEIRO" Title="Boletos" Subtitle="Boletos disponibilizados pela administração para suas unidades.">
    <Actions><MudIconButton Icon="@Icons.Material.Outlined.Refresh" OnClick="LoadAsync" aria-label="Atualizar" /></Actions>
</PageHeader>

<PageState Loading="_loading" Error="@_error" Empty="@(_boletos.Count == 0)" EmptyTitle="Nenhum boleto disponível" EmptyText="Quando a administração publicar um boleto para sua unidade, ele aparece aqui." Retry="LoadAsync">
    <section class="list-group boletos-list">
        @foreach (var boleto in _boletos)
        {
            <div class="list-row boletos-row">
                <span class="settings-icon warning"><MudIcon Icon="@Icons.Material.Outlined.ReceiptLong" /></span>
                <div class="list-main">
                    <div class="list-title">@boleto.Reference</div>
                    <div class="list-meta">@boleto.UnitLabel · Vencimento @boleto.DueDate.ToString("dd/MM/yyyy")</div>
                </div>
                <MudIconButton Icon="@Icons.Material.Outlined.Download" Disabled="@(_opening == boleto.DocumentId)" OnClick="() => OpenAsync(boleto.DocumentId)" aria-label="@($"Abrir boleto {boleto.Reference}")" />
            </div>
        }
    </section>
</PageState>

@code {
    private List<ResidentBoletoViewModel> _boletos = [];
    private bool _loading;
    private Guid? _opening;
    private string _error = string.Empty;

    protected override Task OnInitializedAsync() => LoadAsync();

    private async Task LoadAsync()
    {
        _loading = true;
        _error = string.Empty;
        var result = await Api.GetResidentBoletosAsync();
        _loading = false;
        if (result.Success) _boletos = result.Value ?? [];
        else _error = result.Error ?? "Não foi possível carregar seus boletos.";
    }

    private async Task OpenAsync(Guid documentId)
    {
        _opening = documentId;
        var result = await Api.GetResidentBoletoFileAsync(documentId);
        _opening = null;
        if (!result.Success || string.IsNullOrWhiteSpace(result.Value))
        {
            Snackbar.Add(result.Error ?? "Não foi possível abrir o boleto.", Severity.Error);
            return;
        }
        await JS.InvokeVoidAsync("open", result.Value, "_blank");
    }
}
```

- [ ] **Step 3: Wiring no menu "Mais"**

Em `Condotify.Mobile/Components/Pages/More.razor`, na seção `else` (bloco de morador, dentro de `<span class="settings-label">Condomínio</span>`), adicionar uma linha antes de `<a class="list-row" href="/cameras">`:

```razor
                <a class="list-row" href="/boletos"><span class="settings-icon warning"><MudIcon Icon="@Icons.Material.Outlined.ReceiptLong" /></span><div class="list-main"><div class="list-title">Meus boletos</div><div class="list-meta">Boletos disponibilizados pela administração</div></div><MudIcon Icon="@Icons.Material.Outlined.ChevronRight" /></a>
```

- [ ] **Step 4: Categoria de notificação no mobile**

Em `Condotify.Mobile/Components/Pages/Notifications.razor`, nos três `switch` estáticos, adicionar o caso `Financial`:

```csharp
    private static string Label(MobileNotificationCategory category) => category switch { MobileNotificationCategory.Access => "Acessos", MobileNotificationCategory.Visitor => "Visitantes", MobileNotificationCategory.Delivery => "Encomendas", MobileNotificationCategory.Booking => "Reservas", MobileNotificationCategory.Security => "Segurança", MobileNotificationCategory.Operational => "Operação", MobileNotificationCategory.Financial => "Boletos", _ => "Sistema" };
    private static string IconFor(MobileNotificationCategory category) => category switch { MobileNotificationCategory.Access => Icons.Material.Outlined.Badge, MobileNotificationCategory.Visitor => Icons.Material.Outlined.Group, MobileNotificationCategory.Delivery => Icons.Material.Outlined.Inventory2, MobileNotificationCategory.Booking => Icons.Material.Outlined.CalendarMonth, MobileNotificationCategory.Security => Icons.Material.Outlined.Security, MobileNotificationCategory.Operational => Icons.Material.Outlined.WarningAmber, MobileNotificationCategory.Financial => Icons.Material.Outlined.ReceiptLong, _ => Icons.Material.Outlined.Info };
```

(a linha de `ColorFor` pode ficar como está — `Financial` cai no `_ => Color.Primary` padrão, o que já é adequado.)

- [ ] **Step 5: CSS**

Em `Condotify.Mobile/wwwroot/css/app.css`, adicionar (reaproveitando classes `.list-row`/`.settings-icon` já existentes — só a lista em si precisa de uma regra nova, se `.boletos-list`/`.boletos-row` não coincidirem 1:1 com algo já genérico):

```css
.boletos-list { display: grid; gap: 1px; background: var(--line-soft); border-radius: 16px; overflow: hidden; }
.boletos-row { background: var(--surface); padding: 13px 15px; }
```

- [ ] **Step 6: Build**

Run: `dotnet build Condotify.Mobile/Condotify.Mobile.csproj -f net9.0-android35.0`
Expected: build limpo.

- [ ] **Step 7: Commit**

```bash
git add Condotify.ApiClient/CondotifyApiClient.cs Condotify.Mobile/Components/Pages/Boletos.razor Condotify.Mobile/Components/Pages/More.razor Condotify.Mobile/Components/Pages/Notifications.razor Condotify.Mobile/wwwroot/css/app.css
git commit -m "feat(boletos): add mobile resident view with download and push notification wiring"
```

---

## Verificação final (depois de todas as tarefas)

- [ ] `dotnet build CondotifyAPI.sln && dotnet build Condotify.sln` — build limpo em ambos.
- [ ] `dotnet test CondotifyAPI.Tests` — suíte completa passando (não só os testes novos).
- [ ] Rodar a API + Postgres localmente, aplicar a migração `AddBoletos` num banco de desenvolvimento (nunca produção sem confirmação explícita), e testar manualmente pelo menos um fluxo ponta-a-ponta: subir um PDF de teste com 2-3 páginas (algumas com CPF reconhecível, uma sem nada), revisar, publicar, e conferir que o morador certo vê o boleto certo no app.
