name: condotify

# Ponto de entrada unico da VPS. Cada stack continua autocontida e pode ser
# mantida separadamente, enquanto o .env deste diretorio fornece a
# interpolacao compartilhada para todos os includes.
include:
  - path: ./deploy/proxy/compose.yaml
  - path: ./deploy/condotify/compose.yaml
